# Pi Agent V2 — 界面功能需求文档（设计物料）

> 依据 feature-inventory-for-design 流程整理：通读全部前端组件后，按用户可见界面盘点字段/按钮/状态。
> 本文件不含布局约束与视觉建议（布局/视觉由 frontend-design 阶段决定）。

## 产品概述

Pi Agent V2 是一个 AI 代理工作台：用户在对话里下达任务，模型自主决定直接执行或拆解成节点工作流（画布可视化），最终交付物（文件）通过 Artifact 栏统一查看。核心体验 = 对话驱动 + 执行可视化 + 交付物闭环。

## 基础功能（常驻界面元素）

### 1. 项目选择（首次进入/无项目时）
- 用户场景：开始对话前必须先选定一个项目文件夹（会话按项目归档）
- 字段：项目文件夹路径输入框
- 操作：开始对话按钮；最近项目列表（点击直接进入，最多 5 条，本地记忆）
- 状态：输入为空时开始按钮不可用；无最近项目时不显示该区块

### 2. 会话侧边栏（常驻左侧）
- 用户场景：切换/新建/删除会话；进入技能与 API 配置
- 元素：
  - 「会话」标题 + 新建会话（＋）按钮
  - 会话列表行：显示会话首条消息文本；无消息显示「（空会话）」
  - 每行 hover 出现删除（✕）按钮 → 弹确认框 → 删除
  - 当前会话行有选中态（边框高亮），点击其他行切换会话
  - 底部入口：Skills 按钮、API 按钮
- 状态：无会话时显示「暂无会话」；会话按修改时间倒序

### 3. 顶栏（常驻）
- 元素：项目名、项目路径（超长省略）、Artifacts 按钮、画布按钮（仅当存在节点图时显示）、模板按钮、模式切换按钮
- Artifacts 按钮：显示产物数量计数；点击展开/收起产物条
- 画布按钮：显示节点数；点击展开/收起画布面板；再点节点卡可看详情
- 模式切换按钮：正常模式 ⇄ 简单模式（简单模式 = 裸模型无定制工具）；当前模式有颜色标识；切换会新建会话
- 状态：画布存在与否影响按钮显隐；模式影响按钮文案与颜色

### 4. Artifact 产物条（顶栏展开区）
- 用户场景：查看本次会话产出的交付文件（文档/图片/音频），点击预览
- 元素：产物文件 chip 列表（文件名），点击打开预览弹层
- 状态：无产物时显示「暂无产物文件」
- 产物定义（判定在 backend/artifacts.js）：项目内 + 扩展名白名单（md/doc/docx/pdf/html/图片）+ 产出型节点产物；json/py/csv 等中间文件不收

### 5. 消息区（主操作面）
- 用户场景：与模型对话；看到工具执行过程与最终结果
- 元素：
  - 用户消息：文本气泡
  - 助手消息：可选思考块（「思考」+ 首行预览，点击展开完整思考）+ Markdown 正文（支持代码高亮/表格/公式）
  - 活动条目行：工具执行记录（工具名/节点名文本 + 状态图标），节点执行中带实时心跳流，可展开查看工具参数 JSON
  - plan 结果块：默认折叠为一行摘要（如「已拆解任务：N 个节点」），点击展开完整节点图 JSON
- 状态：模型忙碌时活动行显示动态状态环（搜索中/处理中/工作中）；错误显示 ⚠；空闲显示 ✓
- 空态：无消息时显示产品名 + 引导语「发送一条消息开始——规划、执行、交付，都在对话里」

### 6. 输入区（常驻底部）
- 字段：多行文本输入框（自适应高度）
- 操作：Enter 发送；Shift+Enter 插话（模型忙碌时）；忙碌时按钮变为「停止」
- 状态：空闲=发送按钮（输入为空时禁用）；忙碌=停止按钮

## 弹层面板

### 7. 画布面板（顶栏下拉）
- 用户场景：查看任务被拆解成的节点图与各节点执行状态
- 元素：节点卡片网格（类型 + 名称 + 状态 + 依赖）、节点间依赖连线
- 状态：节点三态 —— 未执行 ○ / 运行中 ⏳ / 已完成 ✓（含产出）/ 卡住 ⚠
- 节点详情弹层：类型/ID/名称、依赖、输入、指令文本、产出内容（Markdown 渲染）、该节点产物文件列表（点击预览）

### 8. 产物预览弹层
- 用户场景：打开任意交付物文件查看内容
- 元素：文件路径标题、关闭按钮、内容区
- 按类型渲染：图片（内联显示）/ 音频（播放器）/ 文本（Markdown 渲染或等宽原文）

### 9. 技能管理面板
- 用户场景：查看/搜索全部技能，启停某个技能（模型是否可自动调用）
- 字段：搜索框（按名称/描述过滤）
- 元素：技能行 —— 启用开关（toggle）、技能名、作用域标签（global/project/path）、描述（单行省略）、文件路径（等宽小字）
- 操作：toggle 开关；关闭（✕）；点击遮罩关闭
- 状态：禁用技能行降透明度；标题显示技能总数；无匹配时显示「无匹配 skill」
- 操作后果：禁用/启用会改写 SKILL.md frontmatter（持久化，跨会话生效）

### 10. 模型与 API 配置面板
- 用户场景：查看可用模型供应商，配置/覆盖 API Key
- 元素：provider 块 —— 名称 + 认证状态（✓ 已认证 / 未配 API Key）、模型名列表（reasoning 模型带 ⚡ 标记）、API Key 密码输入框 + 保存按钮
- 操作：输入 Key 保存（写入 AuthStorage，跨会话生效）；输入为空时保存按钮禁用
- 状态：标题显示 provider 总数；无 provider 时显示空态

### 11. 模板库面板
- 用户场景：把跑通的节点图存为模板复用，或载入既有模板执行
- 字段：模板名输入框（保存当前图为模板）
- 元素：模板行 —— 标题 + 「N 节点 · 保存时间」+ 载入执行按钮
- 状态：无模板时显示引导空态

## 状态与流转

| 状态 | 含义 | 用户可做 |
|---|---|---|
| 未选项目 | 尚无项目会话 | 输入路径/选最近项目 |
| 会话空闲 | 无任务运行 | 输入消息 / 操作各面板 |
| 会话忙碌 | 模型正在执行（直接执行或节点运行中） | Shift+Enter 插话 / 停止 / 看节点实时流 |
| 节点未执行 ○ | 图中尚未轮到 | — |
| 节点运行中 ⏳ | 正在执行，有实时心跳 | 展开节点详情看流 |
| 节点已完成 ✓ | 有产出 | 看产出 / 预览产物文件 |
| 节点卡住 ⚠ | 执行异常 | 查看详情，指令模型继续 |

## 核心体验原则

1. 对话是主入口：一切任务从一条消息开始，过程与结果都在对话流里
2. 执行可见：模型调了什么工具、节点跑到哪一步，用户始终能看到
3. 交付物闭环：产出的文件有统一入口（Artifact 栏），点开即预览
4. 运行可打断：忙碌时可插话、可停止
5. 配置可逆：技能开关、API Key、模板保存都有明确后果且可反悔
6. 模式分明：正常模式（全能力）与简单模式（裸模型）边界清晰
7. 会话持久：断点续跑，重开即恢复最近会话

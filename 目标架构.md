# Pi Agent V2 — 目标架构

> 2026-08-07，承接《架构现状.md》与上轮讨论
> 原则：简洁优雅、可解耦就解耦、依赖最少。以 pi 自身设计为模板（一个 agent 循环 + 工具 + JSONL 会话）。

---

## 0. 设计目标（用户已确认）

```
一个主对话界面（沿用现有 Session UI）
  ├─ 正常模式：Workflow + Execute 融合
  │    模型自主决策：
  │      ├─ 需操作软件/环境的任务 → 直接在对话里调工具干（Fable 5 模式）
  │      └─ 纯输入输出型任务 → 自己生成多节点工作流
  │          画布（照搬 WorkflowEditor 交互）显示在对话页右上角
  └─ 简单模式按钮（输入框右侧）：纯净 pi——裸模型 + 默认工具，无任何定制提示词
```

- 三种旧界面收敛为：**一个对话页 + 两种模式**。Session（纯净模式）由简单模式按钮保留。
- 能力一个不丢，丢的是实现它们的零件数量。

## 1. 总体骨架（pi SDK 为后端的 90%）

```
前端（React，沿用现有 AppShell/ChatWindow 组件）
  │  WebSocket/SSE 事件流 + 命令
后端（极薄，~800 行自定义，其余全是 pi SDK）
  ├─ session-server.js（薄 HTTP/WS：会话 CRUD、转发事件、转发命令）
  ├─ 自定义工具 5 个（defineTool 注册进 agent）
  │    ├─ plan 工具（弱模型生成节点图）
  │    ├─ worker 工具（按图执行节点）
  │    ├─ subagent 工具（并发多 agent）
  │    ├─ browser 工具（现有 execution-tool-runtime 收编）
  │    └─ gather 工具（现有调研收集逻辑收编）
  └─ pi SDK（不动，直接依赖）
       ├─ createAgentSession / createAgentSessionRuntime   ← agent 循环 + 会话生命周期
       ├─ createBashTool / createCodingTools / createReadOnlyTools  ← 现成工具
       ├─ SessionManager（JSONL 事件流，断点续跑天然成立）
       ├─ ModelRegistry + AuthStorage（模型路由 + 凭证）
       ├─ session.subscribe（事件流 → 前端）
       ├─ steer / followUp（流式时用户插话）
       └─ compact（原生压缩，不再自己写截断/过滤/重组）
```

**原 monitor-server.js 的 14k 行去哪了**：
| 现状职责 | V2 归属 |
|---|---|
| agent 循环、重试、压缩 | pi SDK（原生，且更完善） |
| 会话持久化 | pi SessionManager（JSONL） |
| orchestrate 状态机（guardian/plan/replan/DAG） | 删。plan+worker 变成两个工具 |
| masterSession 内存态 | 删。会话 = JSONL |
| 模型路由 | ModelRegistry + 工具内部按需调用弱模型 |
| 44 个 API 端点 | 剩 ~10 个（会话 CRUD + 事件桥） |

## 2. 后端模块清单（全部自定义代码）

| 模块 | 文件 | 职责 | 边界（不做什么） |
|---|---|---|---|
| **会话服务** | `server.js` | 会话 CRUD、事件桥接（pi 事件 → WS 推前端）、命令转发（prompt/steer/abort/新建/恢复/删除） | 不做任何规划/判断/调度 |
| **plan 工具** | `tools/plan.js` | 入参：用户目标 + 材料；调弱模型生成节点图（节点 = 干净上下文 + 明确动作）；输出图 JSON | 不自己执行节点 |
| **worker 工具** | `tools/worker.js` | 入参：节点图 + 节点 id；按图执行单节点（干净上下文 + 调工具/子模型）；产出 → 写入会话事件 | 不做全局调度，一次一步 |
| **subagent 工具** | `tools/subagent.js` | 入参：任务 + 上下文；并发开 N 个 pi AgentSession（各自独立工具循环）；返回结果摘要 | 不做宿主分发，模型自己决定开几个 |
| **browser/gather 工具** | `tools/browser.js` | 现有 execution-tool-runtime 的浏览器/文件/桌面副作用层，原样收编 | 不改逻辑，只拆出 |
| **会话目录** | `sessions/` | 复用现有前端会话列表的数据源 | — |

依赖清单：`@earendil-works/pi-coding-agent`（唯一新增，替换掉 express/ws/monitor 全家桶）+ 前端现有 React/Next 依赖。

## 3. 前端模块清单

| 模块 | 来源 | 说明 |
|---|---|---|
| ChatWindow | 照搬现有 | 主对话，消息渲染/流式 |
| 简单模式按钮 | 新增小按钮 | 输入框右侧；切到"裸 pi"配置 |
| 右上角画布 | 照搬 WorkflowEditor | 只读/进度模式：节点状态、点节点看材料与结果；编辑另开 |
| 会话侧边栏 | 用现有 SessionSidebar | 删掉 Execute 简化版侧边栏，只留一个 |
| 会话状态 hook | 重写为 `usePiSession` 一个 | 替换 useAgentSession + useOrchestrate 两套 |

前端只依赖一个后端协议：`/api/sessions/:id/events`（WS 事件流）+ 少量命令端点。删掉 29 个 app/api route 中直读文件与代理 3000 混用的那套。

## 4. 数据模型

**会话 = pi 原生 JSONL 事件流**（一种格式，全模式共用）：
```
{"type":"session_header", ...}                        # 会话元信息（模型、cwd、模式）
{"type":"message", ...}                               # 用户/助手消息（模型自己管上下文）
{"type":"tool_call", ...}                             # 工具调用（plan/worker/browser/...）
{"type":"tool_result", ...}                           # 工具结果
{"type":"custom", "key":"node_graph", ...}            # plan 输出的节点图（CustomEntry，结构化 JSON）
{"type":"custom", "key":"node_progress", ...}         # worker 每步进度事件
{"type":"compaction", ...}                            # pi 原生压缩
{"type":"label", ...}                                 # 节点打标（可选，跨会话恢复点）
```

- **节点图 = 会话内的一条 custom 事件**，不是独立存储。断点续跑 = 重读会话文件找最近 node_graph 事件，天然成立。
- **节点进度 = 追加事件**，前端画布从事件流渲染，无需单独同步状态。
- **三套会话（agent jsonl / masterSession / ledger）→ 一套**，旧文件在迁移时转成 pi JSONL。

## 5. 能力 → 模块映射表（确保一条不丢）

| 现有能力 | V2 归属 |
|---|---|
| 长任务持续执行（mission wakeup/continuation） | JSONL 事件流 + agent 循环。断点续跑替代定时器唤醒 |
| 多 Agent 协作（execution node + worker 分发） | subagent 工具（模型并发开，真并行） |
| 弱模型规划（stage judgment + planner） | plan 工具（输出收敛为"一张节点图"） |
| 按图执行（节点干净上下文） | worker 工具（每次调用只带该节点材料） |
| 工具运行时（browser/文件/桌面/mcp） | browser/gather 工具 + pi createBashTool 等 |
| Workflow 图编辑/复用 | WorkflowEditor 画布（右上角展示 + 图存为 custom 事件可复用） |
| 上下文管理（原 masterSession 截断/过滤/重组） | pi 原生：模型自管 + compact 压缩 |
| 会话/模型/凭证 | SessionManager + ModelRegistry + AuthStorage |
| Session 纯净模式 | 简单模式按钮（同 agent 循环，无定制提示词配置） |
| guardian（失败兜底） | plan 工具异常分支："生成不了图就明说搞不定" |
| project-memory | 融进会话事件流，模型按需回溯（砍独立机制） |

## 6. 砍掉清单

- monitor-server.js（14k 行 orchestrate 状态机全家桶）
- masterSession / pendingReplan / recompileMasterPlanAtBoundary 链路
- train、profile 系统、guardian、project-memory 独立机制
- 三套会话模型（agent jsonl / masterSession / workflow ledger）
- 前端 useAgentSession + useOrchestrate 双 hook（→ usePiSession 一个）
- Execute 简化版侧边栏（重复的 SessionSidebar 逻辑）
- 29 个 app/api route 的直读/代理混用层

## 7. 三个决策（已拍板，2026-08-07）

1. **简单模式 = 会话级**：新建会话时选，实现最简。
2. **节点图复用 = 保留，形式是模板库**：用户确认的模板 = "哪类节点用了什么 profile/skill + 初始提示词"。现有机制是 `execution-profiles.json`（7 个 profile：form.fill / tool.mcp / code.generate / content.generate / communication.email / software.operation / generic.execution）→ 每节点挂 `executionProfileId` → profile 带 `skillIds`（固定技能）+ 节点可配 configurable skills。V2 落地：node_graph 事件的每个节点带 `profileId + skillIds`，"保存为模板" = 把一张已跑通的节点图（含标注）写入模板库目录 `templates/`，复用 = 从模板库载入节点图再跑。
3. **工具 = 尽量保留（执行时修正）**：18 个 BUILTIN_SCHEMAS 的实际处置——文件×5（read/ls/find/grep/write）用 pi 原生工具（零收编）；**浏览器×6（browser×5 + ego_browser_run）砍掉**——与 ego-browser（skill + CLI）重叠，ego-browser 是 pi 生态原生（全局 skill + `~/.local/bin/ego-browser` 已装），模型用 bash 调即可，主 agent 和 worker 都能用；已收编 `http_request` + `execution_app_snapshot`（tools/runtime-tools.js）；mailpit×3 / calendar×2 / mcp__* 按需再收。MCP 工具前缀（mcp__）将来保留。

## 8. UI 保留清单（用户确认，2026-08-07）

> 用户逐个过旧界面后的拍板：以下三个界面的具体功能保留进 V2 统一对话页。

| 旧界面 | 保留功能 | 代码位置（助手加思考融合版） | V2 归属 |
|---|---|---|---|
| Session | Skill / API / 顶上 Artifact | `AppShell` 动态面板 `SkillsConfig`/`ApiGuide`/`ModelsConfig` + `WorkflowArtifact` 产物条 | 统一对话页保留配置面板（简单模式除外，但不删配置入口）；工具产物（截图/生成文件）继续以 artifact 条渲染在对话顶部/画布 |
| Workflow | 预览功能 | `WorkflowEditor` `artifactPreview` 弹层（document/image/audio）+ `previewText/previewBlock` 材料预览 | 右上角画布：点节点看材料与结果（复用 artifactPreview 弹层与 previewText 助手） |
| Execute | 合并消息逻辑 | `SimpleAssistantDialog` merge：masterSession 消息 + 进度事件 + 乐观条目按 time 排序，running 活动与其 returned 结果合并为一条 | `usePiSession` 的消息投影层（唯一会话 hook，行为照搬） |

**合并消息逻辑要点（照搬进 usePiSession）**：三源合并（持久消息 / 进度事件 / 乐观本地条目）→ 按 `time + sequence` 排序 → 相邻同 title/taskId/toolName 的 running 条目与返回条目折叠成一条。

## 11. 12 类节点融入方案（2026-08-07 聊清楚）

> 用户提问：workflow 里 12 类节点 + 每类节点的提示词，怎么融入 V2？

**现状盘点**（旧系统）：
- 12 类节点 = `monitor-server.js` 的 `NODE_TYPE_SKILLS`：Fetch/Gather、Standardize、Classify/Route、Extract/Validate、Generate/Draft、Transform/Produce、Analyze/Judge、Strategize/Plan、Writeback/Action、Review/Gate、Artifact/Render、Monitor/Alert，每类映射一个 toolkit skill（`skills/node-<类名>-toolkit/SKILL.md`）
- 每类 SKILL.md 统一结构：最高优先级 / 触发条件 / 可用能力选择逻辑 / 输出要求 / 禁止
- 节点 prompt 组装（monitor-server `loadSkillPromptBlock`）：**运行时把 SKILL.md 压缩拼进 prompt 字符串**，加 profile skillIds（7 个 action-*）+ 节点自定义字段（materialOutput/methodHint/qualityBar）→ 塞模型上下文。token 代价大、易超限

**V2 融入方式**（核心：提示词资产与代码解耦）：
1. **12 类节点分类体系保留**，定义表变成静态映射（`tools/node-types.js`，不再埋在 14k 行文件里）
2. **12 个 toolkit SKILL.md + 7 个 profile action skill + execution-reasoning 等 = 纯文本资产原样收编**，复制到 V2 `skills/` 目录（0 新依赖，改提示词 = 改文件，不动代码）
3. **组装方式换成 pi 原生 skill 机制**：worker 工具执行节点时只给节点带 `skillIds`（toolkit + profile），模型**按需读取** SKILL.md——不再压缩拼字符串，旧系统 token 超限问题消失
4. 节点图里每节点带 `nodeType + profileId + skillIds`（与 §7 决策 2 模板库衔接：保存模板 = 带标注的节点图）
5. 前端：WorkflowEditor 的节点编辑 UI 暂不搬（V2 画布先只读展示节点状态/材料/结果；节点编辑走对话由模型生成节点图）

**节点执行与调度（2026-08-07 补充，用户追问后定稿）**：

- **执行结构（三层，2026-08-07 定稿）**：
  - 模型（主 agent 循环）= 调度器：上下文 = 节点图 + 各节点摘要（不膨胀），看图决定下一个执行哪个节点，调 worker(node_id)
  - worker 工具 = 单节点执行器：内部一个**常驻轻量 AgentSession**（`SessionManager.inMemory` + 弱模型 + 全工具），每节点执行前**替换上下文** `agent.state.messages = [系统(核心目标+skill+prompt), 用户(交接物料)]` → 一次 prompt() → 结果写回主会话 `node_output` 事件 → 返回摘要给主 agent。隔离靠替换而非新建（SDK 官方支持 state.messages 替换）
  - 12 类节点 = 节点类型标签 → 决定注入哪份 toolkit skill（提示词资产）
- **干净上下文 = 上下文替换天然成立**：节点执行者只见自己需要的物料 + skill + prompt，不见全量历史；主 agent 只见摘要 → 旧系统"masterSession 160 条截断导致失忆"问题不存在
- **不用每节点开新会话**：模型调用次数躲不掉（每节点一次推理），但会话只建一次，反复用；节点跑弱模型（比主模型便宜）
- **交接**：子 agent 产出写回主会话 `node_output` custom 事件；下游节点的 worker 只从事件流取自己 `input` 声明的物料
- **worker 不做全局调度**（不做拓扑推进）：worker 内部跑循环自动执行全图 = 变成新 orchestrate 状态机，旧系统 14k 行死因，明确不做；依赖未满足时 worker 拒绝执行并提示模型先跑上游

**依赖控制承诺**（用户要求依赖少）：
- backend 仅 pi-coding-agent + ws（不动）
- frontend 不再新增依赖（markdown 渲染全家桶已装；mermaid 已砍）
- 12 类节点技能 = 文件复制，零依赖

## 13. 进度三（2026-08-07，skills 收编 + plan/worker 工具）

- ✅ **skills 收编**：12 个 node-*-toolkit + 6 个 action-* + execution-reasoning + execution-complexity-judgment 共 21 个 SKILL.md 复制到 `skills/`（84K，零适配，无旧系统引用）；`execution-profiles.json` 同步复制
- ✅ **worker 隔离机制实测验证**（`worker-isolation-test.mjs`）：`agent.state.messages` 替换后模型完全不知道上一轮内容 → 干净上下文成立
- ✅ **plan 工具**（`tools/plan.js`）：调 worker 会话生成节点图 JSON，或判定 `{"direct":true}` 不拆图（实测：小任务正确判定 direct，多步任务正确拆 3-5 节点图）
- ✅ **worker 工具**（`tools/worker.js` + `worker-session.js`）：常驻 in-memory 会话 + 互斥锁（plan/worker 共用串行化）；每节点执行前替换上下文（只注入节点 prompt + 12 类 toolkit skill + 交接物料）；产出 `appendCustomEntry("node_output")` 写回主会话（custom entry 不参与主 agent 上下文）；单测 `worker-test.mjs` 验证依赖拒绝 + 物料传递
- ✅ **端到端实测**：3 节点图（生成→整理→分析）worker 逐个独立执行，物料 CSV→clean_data.json→conclusion.json 逐级交接，主 agent 只调度
- ⚠️ 已知限制：worker 会话暂无搜索/浏览器工具（Fetch/Gather 类节点靠 bash 或未来收编）；弱模型路由未做（先用默认模型）
- ⏭ 下一步：画布只读版（node_graph/node_output 事件 → 前端渲染）→ 浏览器/gather 工具收编

## 14. 进度四（2026-08-07，画布只读版）

- ✅ plan 工具把图写进主会话 `node_graph` custom 事件（`getSession().sessionManager.appendCustomEntry`）；worker 产出继续写 `node_output`
- ✅ 后端新增 `get_graph` 命令：返回最新 node_graph + **该图之后**的 node_output（按 entry index 过滤，防跨任务残留）
- ✅ 前端 `usePiSession` 加 graph 状态：ready/plan/worker 工具事件后自动刷新；`tool_execution_start(worker)` 标记 running 节点
- ✅ `components/NodeCanvas.tsx`：拓扑分层布局（deps 计算层号）+ 类型配色 + 状态 ✓/⏳/○ + 点击节点弹层预览（类型/依赖/输入/指令/产出）
- ✅ 浏览器实测：3 节点任务全 ✓、running 状态、预览弹层（产出 1407 字符）
- ⏭ 下一步：Skill/API 配置面板 → 浏览器/gather 工具收编 → 弱模型路由

## 15. 进度五（2026-08-07，工具收编第一批）

- ✅ **http_request**（tools/runtime-tools.js，fetch 封装）：worker 实测请求 health 返回 200/ok
- ✅ **execution_app_snapshot**（tools/runtime-tools.js + scripts/macos-accessibility-control.mjs 原样搬）：worker 实测快照 Finder（进程存活/零窗口/三路验证）
- ✅ **浏览器能力 = ego-browser**：与 browser×5/ego_browser_run 重叠，工具都不收编；ego-browser 是 pi 全局 skill + CLI（`~/.local/bin/ego-browser` 已装），模型用 bash 调，主/worker 都会发现全局 skill（决策 3 已原地修正）
- ✅ **文件类 = pi 原生**（read/bash/write/grep/find/ls，零收编）
- 未收编（按需）：mailpit×3 / calendar×2 / mcp__*（依赖外部服务配置）
- 实测行为亮点：模型自主决策（plan direct 判定）+ 用户指令优先级（强制拆图也照做）；快照失败时三路独立验证排除误报；不擅自动用户桌面
- ⏭ 下一步：Skill/API 配置面板 → 简单模式按钮 → subagent 工具

## 16. 进度六（2026-08-08，模型路由）

- ✅ **统一模型 = deepseek-v4-flash**（用户拍板："强弱都这个模型够用了"）：主 agent 默认 flash（server.js `MAIN_MODEL` 可覆盖），worker 弱模型优先级第一位 flash（`WORKER_MODEL` 可覆盖）
- ✅ 实测：主 agent 直接任务、plan direct 判定、worker 拆图执行全链路在 flash 下正常，行为质量保持（独立重算验证、口径提醒）
- ⏭ 下一步：Skill/API 配置面板 → 简单模式按钮 → subagent 工具

## 17. 进度七（2026-08-08，简单模式）

- ✅ **简单模式 = 会话级**（决策 1 落地）：`new_session {mode}` 命令，simple 模式 createAgentSession 不注册 plan/worker 工具（裸 pi + 默认工具）
- ✅ 前端：header 简单模式按钮，点按新建会话并切换；ready 事件带 mode 同步状态；绿色 ✓ 表示当前 simple
- ✅ 实测：simple 会话直接对话（无工具活动）、切回 normal 正常
- ⏭ 下一步：Skill/API 配置面板 → subagent 工具 → 模板库

## 18. 进度八（2026-08-08，subagent）

- ✅ **subagent 用 pi 官方扩展，不自己写**（用户提醒："pi 本身有 subagent"）：`examples/extensions/subagent` 复制到 V2 `.pi/extensions/subagent/`，精简版砍 TUI 渲染（省 pi-tui 依赖），保留 single/parallel/chain 三模式 + agents 发现 + 独立 pi 进程隔离 + usage 统计 + abort
- ✅ agents 定义：V2 `.pi/agents/`（project scope）：planner / reviewer / scout / worker 四个，model 统一 `opencode-go/deepseek-v4-flash`（原官方定义是 claude-sonnet/haiku，无 key 跑不了）
- ✅ 实测：parallel 模式 2/2 成功（agent=worker，agentScope=project）；模型自主发现 scope 并诊断 key 问题
- ⚠️ 踩坑：官方 `getPiInvocation` 读 `process.argv[1]`（假设入口是 pi 本体）→ 在 `node server.js` 场景会 spawn 二次后端（端口冲突卡死）；V2 直接 spawn `pi`。另：continueRecent 恢复的旧会话工具列表不含新扩展工具，需新会话
- ⏭ 下一步：Skill/API 配置面板 → 模板库

## 19. 进度九（2026-08-08，Skill 面板）

- ✅ **后端**：`get_skills`（session.resourceLoader.getSkills()，pi 原生）`set_skill_disabled`（搬旧 setDisableModelInvocation，改 SKILL.md frontmatter）
- ✅ **skills 资产归位**：V2 skills/ 移到 `.pi/skills/`（pi 项目级发现路径）；21 个 SKILL.md 补 frontmatter（name/description）——踩坑：description 含冒号（`Action Profile: Content Generation`）导致 YAML 解析失败静默跳过，双引号包裹修复
- ✅ **前端**：SkillsPanel 照搬旧 SkillsConfig 的 toggle 交互（开关 + 来源标签 + 路径 + 搜索），接 WS 命令
- ✅ 实测：47 个 skill（26 全局 + 21 V2）列出；toggle 双向写 frontmatter（后端直测 skill_toggled ✓）
- ⚠️ 小坑：浏览器 .click() 不触发 React 合成事件（需原生 dispatchEvent）；skill_toggled 响应有时序延迟
- ⏭ 下一步：API/模型配置面板（ApiGuide）→ 模板库

## 20. Stage 收集阶段设计（2026-08-08 用户确认）

> 用户澄清：原 workflow 两阶段必须兼容——Stage（收集需求）与 workflow 节点（执行）。V2 主会话流程 = 识别 → 收集 → 拆图 → 执行。

**流程（逐步收缩的意图漏斗）**：
```
用户消息
 → 识别：纯输入输出？单步任务？→ 直接答
         需要 workflow 方案 / 调查？→ 进入收集阶段
 → 收集（Stage 1，可多轮）：
      ├─ 目标推断链：显式目标 → 隐藏目标 → 衡量指标
      │    例："策划婚礼" → 隐藏目标"办好婚礼" → 衡量指标"亲友满意/成本低/不出岔子"
      ├─ 每轮只问 1 个最小问题（userBlockingQuestions），收敛后不再问
      └─ 收敛条件：stage2Ready（意图已够清晰，剩余未知可环境发现或中性默认）
 → 拆图（Stage 2）：节点图生成时，每节点绑定其服务的衡量指标
 → 执行：worker 逐节点，产出可对照指标验收
 → 最终产物验收标准 = 衡量指标达成（successCriteria）
```

**Stage 1 提示词结构（收编原 stage1Prompt 资产 + 用户补充）**：
1. **目标推断链**：显式目标 → 隐藏目标（用户真正要的结果）→ 衡量指标（怎么算成功：亲友满意/成本/风险等）
2. **逐步收缩询问**：每轮最多 1 个最小问题；已收敛维度不再追问；纯交流（conversation）直接回复不进入收集
3. **指标驱动生成**：workflow 节点生成时每节点标注服务指标；最终产物验收标准 = 指标达成
4. **convergence 状态机**：ready / needs_user / needs_discovery（原逻辑保留）

**节点状态流转（V2 五态，前端画布）**：
| 状态 | 来源 |
|---|---|
| 待运行 pending | 图中有节点、无产出 |
| 运行中 running | `tool_execution_start(worker, nodeId)` |
| 完成 done | `node_output` 出现 |
| 阻塞 blocked | worker 返回"依赖未满足" |
| 失败 failed | worker 返回错误（isError） |

**DAG 图显示不受影响**：图 = node_graph 数据（节点 + deps 边），worker 合并不改数据结构，画布拓扑分层照旧；增强：deps 连线箭头（流转方向）+ 当前节点高亮。

## 21. Stage 实现结果（2026-08-08）

- ✅ plan 两阶段化（tools/plan.js）：Stage 1 意图判断（conversation / needsClarification / 可规划三形态）→ Stage 2 拆图
- ✅ **信息四分类边界**（用户诊断后加入）：①方案阻塞型→必须问（每轮 1 问）②可推断型→默认+标注 ③可 gather 型→转 Fetch/Gather 节点 ④物料索取型→图带 requiredMaterials，运行前索要
- ✅ 意图推断链：显式目标→隐藏目标→衡量指标（successCriteria 3~5 条），节点带 serves 字段绑定指标
- ✅ worker 支持 materials 参数（用户提供的 requiredMaterials 物料并入节点上下文）
- ✅ JSON 解析修复：平衡大括号提取（原正则遇长输出截断失败）
- ✅ 实测：模糊目标→needsClarification（问"要方案还是执行"）；完整信息→拆图 6 节点+5 指标；审核发票→拆图+requiredMaterials 索要发票；端到端：执行到依赖用户决策处停下列出待确认项
- ⏭ 剩余：画布五态（pending/running/done/blocked/failed）+ deps 连线；API/模型配置面板；模板库

## 22. 三态画布 + 不造控制命令（2026-08-08 用户拍板）

- ✅ **节点三态**（用户拍板，不要五态）：未激活 ○ 灰 / 工作中 ⏳ 蓝 / 卡住 ⚠ 红（含失败与依赖未满足）；完成 = 有产出（点节点看内容），不单设态
- ✅ **不搞 pause/resume/rerun 命令**（用户拍板）：pi 式设计——用户说"继续"或新任务，模型看会话历史自主判定接续还是新项目。JSONL 事件流天然支持
- ✅ "继续"实测：Stage 1 收集参数 → 用户回复带参数+说"继续" → 模型识别原任务 → 拆图 8 节点 → 继续执行（无任何命令）
- ✅ 前端实现：hook 维护 worker toolCallId→nodeId 映射，失败节点记入 blockedNodeIds；NodeCanvas 三态渲染
- ⏭ 剩余：API/模型配置面板；模板库

## 23. Execute 补齐（2026-08-08，进度流 + 侧边栏）

- ✅ **进度文本流**（progressUpdates 的 V2 形态）：worker 工具事件 → 对话里出现"⏳ 节点 n2：整理数据（Standardize）"→ 完成折叠为"节点 n2：整理数据 完成"/失败"卡住"——比旧系统更结构化（旧是模型自述文本）
- ✅ **会话侧边栏**（SessionSidebar 简化版）：列表（时间/条数/首消息）、切换、删除（confirm）、新建
  - 后端：`list_sessions`（SessionManager.list）/ `switch_session`（open 指定文件，无需 AgentSessionRuntime）/ `delete_session`
  - 历史重放：open 后发 `history`（session.agent.state.messages → PiEntry），切换/恢复会话即见全量历史
- ✅ 实测：切换婚礼会话 → 40 条历史完整恢复
- ⏭ 剩余：API/模型配置面板；模板库

## 25. 斜杠命令层 + 分支（2026-08-10，pi 原生能力补齐）

> 背景：用户对照 pi 原生（/model /compact /branch 等）查 V2 差距——SDK 方法全有（AgentSession.setModel/compact/setSteeringMode + SessionManager.createBranchedSession），缺的是命令 UI（TUI 层不导出）。全部以斜杠命令 + 单按钮补齐。

- ✅ **斜杠命令**（输入框 `/` 开头前端拦截——SDK prompt() 会把 / 当扩展命令/模板展开，不能发模型）：`/help` `/model <关键词>`（setModel 模糊匹配 provider/模型名子串，命中写 model_change 条目 + currentModel 持久，new_session 沿用）`/compact [说明]`（SDK 原生压缩，compaction_start/end 事件自动走前端进度）`/queue on|off`（setSteeringMode+setFollowUpMode 都切 all）
- ✅ **分支**（用户拍板按钮式）：消息行「↳ 分支」按钮（仅 history 重放消息有 entryId 时显示）→ `createBranchedSession(entryId)` 复制根→分支点路径为独立会话文件并切过去（原会话保留，可递归分支）；history 重放按序 zip sessionManager message entry id → entryId
- ⚠️ 踩坑：
  1. **AgentSessionRuntime vs AgentSession**：result.session 是 AgentSession（方法直接在它上，setModel/compact/setSteeringMode），`session.agent` 只是内部 Agent（state.messages 在这读）——开始时写 `session.agent.setModel` 直接崩
  2. **createBranchedSession defer 写盘**：分支文件含 assistant 消息才立即写盘；user-only 分支（分支点前全是 user 消息）文件未落盘 → open 读不到 → `session.sessionManager._rewriteFile()` 强制写盘（私有方法，升级 SDK 时验证是否已自动写）
  3. **旧 server 进程占端口**：4700 有旧代码 server 在跑，test 一直连旧的 → 排查半天；改动后端先 `pkill -f "node server.js"`
- ✅ 验证：`branch-test.mjs`（sessionManager 层：内容裁剪/继续写挂点/递归/user-only 落盘）+ `commands-test.mjs`（WS 层真实 LLM：四命令 + entryId + 分支历史裁剪）+ 浏览器实测
- ⏭ 剩余：API/模型配置面板；模板库

## 24. Fable 5 差距补齐（2026-08-08，定时唤醒 + 浏览器注入）

> 用户对照 Claude Fable 5 场景（给目标→自主调研→选型→浏览器操作后台→提交工单→等回复→继续→交付→提醒）评估 V2。

- ✅ **wait_for 定时唤醒工具**（tools/wait.js，pi 式：能力=工具，模型自主调用）：模型需要等外部事件（邮件/工单/审批）时调 wait_for{minutes,note} → 工具立即返回（不阻塞 agent 循环）→ server 定时器到点 `session.followUp(note)` 自动唤醒 → agent 继续。新会话/切换会话清理定时器
- ✅ **ego-browser 收编**（.pi/skills/ego-browser）：worker/主 agent 的资源发现能看到它（22 个 V2 skills），模型按需读取使用；实测打开 example.com 报告标题+内容
- ✅ **Fable 5 能力对照**：自主规划/并行调研(subagent)/自主选型/浏览器操作(ego-browser)/文档交付 全部具备；缺"数小时无人值守"——wait_for 补上等待段，断点续跑补上跨天段
- 旧 Execute 的 mission wakeup（定时唤醒）与邮件等待 = wait_for 工具继承；比旧系统更简（工具即能力，无独立 wakeup 机制）
- ⏭ 剩余：API/模型配置面板；模板库

## 12. 依赖与前端取舍（2026-08-07 用户拍板）

- 前端后续会整体重构，现阶段**不为前端投入过多**：画布/面板只做最小可用版
- 依赖只减不增：任何新功能先问"能否用已有依赖/pi 原生能力实现"
- 前端能砍就砍：不搬 ChatInput（861 行）、ChatMinimap、SessionSidebar 全量；

---

## 9. 进度（2026-08-07，最小闭环已完成并验证）

- ✅ **后端** `backend/`：WS 事件桥 + pi SDK agent 会话（`server.js` ~110 行）。命令：prompt/steer/followUp/abort/new_session；断点续跑 = 重连即 `continueRecent`。冒烟测试 `node test-client.mjs` PASS。
- ✅ **前端** `frontend/`：Next.js（端口 3102）+ `usePiSession` hook + 极简聊天页。
  - 事件投影：`message_update/end/turn_end` 的 assistant 消息**无 id**，按 turn 自增序号合并（pi SDK 事件设计）；工具活动 running→done 折叠（Execute merge 简化版）。
  - 已测：流式渲染 ✓ 工具调用活动 ✓ 新会话清空 ✓ Shift+Enter steer 插话 ✓
  - 踩坑：Next dev 被 127.0.0.1 访问时 HMR 被跨域拦截导致水合失败 → `allowedDevOrigins` 解决。
- ⏭ 下一步：接回现有 ChatWindow / 右上角画布 / Skill·API 配置面板（保留清单 §8），浏览器与 gather 工具收编。

## 10. 进度二（2026-08-07，消息渲染层接回）

- ✅ **渲染核心照搬**：`components/MarkdownBody.tsx` + `lib/{types,normalize,markdown}.ts` 从旧前端复制。改动仅两处：
  - `useTheme` 固定 `isDark = true`（V2 暗色，不搬主题 store）；砍 MermaidBlock（动态 import mermaid 大依赖，源码渲染兜底）。
  - 新依赖：react-markdown / react-syntax-highlighter / rehype-katex / remark-gfm 等 + `katex/dist/katex.min.css`（layout 全局）。
- ✅ 已验证：markdown ✓ 代码高亮+行号+copy ✓ katex 公式 ✓ thinking 折叠（默认折，开关展开）✓ 工具活动 ✓ streaming 节流（MarkdownBody 自带 80ms）✓
- ❌ 未照搬：ChatWindow 壳（536 行，依赖 useAgentSession 的 fork/branch/compact 等 V2 已砍概念）、ChatInput（861 行）、ChatMinimap。极简页保持自绘（输入框+Shift+Enter steer+停止+新会话）。
- ⏭ 下一步：右上角画布（WorkflowEditor 只读模式 + artifactPreview 弹层）→ Skill/API 配置面板 → 浏览器/gather 工具收编。

---

下一步（原计划，已完成）：从后端 `server.js` + `usePiSession` 骨架搭起（最小闭环：一条消息 → pi agent 循环 → 前端流式渲染）。


(async () => {
  if (window.__starBg) return 'already';
  window.__starBg = true;
  for (const c of document.querySelectorAll('canvas')) c.style.visibility = 'hidden';
  const e1 = document.createElement('canvas'); e1.id='__bgScene';
  Object.assign(e1.style,{position:'fixed',inset:'0',width:'100%',height:'100%',zIndex:'0',display:'block'});
  const e2 = document.createElement('canvas'); e2.id='__bgLabels';
  Object.assign(e2.style,{position:'fixed',inset:'0',width:'100%',height:'100%',zIndex:'1',pointerEvents:'none'});
  const e3 = document.createElement('div');
  Object.assign(e3.style,{position:'fixed',inset:'0',zIndex:'2',pointerEvents:'none',
    background:'linear-gradient(180deg, rgba(16,16,18,.62) 0, rgba(16,16,18,.22) 18%, rgba(16,16,18,.22) 60%, rgba(16,16,18,.84) 100%)'});
  const e4 = document.createElement('div'); e4.id='__bgNote';
  Object.assign(e4.style,{position:'fixed',right:'20px',bottom:'14px',zIndex:'12',
    font:'11px/1.7 ui-monospace,Menlo,monospace',color:'#8b93a1',whiteSpace:'pre',
    background:'rgba(18,18,20,.78)',border:'.5px solid rgba(255,255,255,.14)',borderRadius:'10px',
    padding:'8px 12px',cursor:'pointer',backdropFilter:'blur(8px)'});
  const e5 = document.createElement('div'); e5.id='__bgFail';
  Object.assign(e5.style,{display:'none',position:'fixed',left:'50%',top:'50%',transform:'translate(-50%,-50%)',
    zIndex:'99',background:'rgba(60,20,20,.94)',border:'1px solid #b91c1c',color:'#fecaca',
    borderRadius:'12px',padding:'16px 20px',font:'12px/1.7 ui-monospace,monospace',whiteSpace:'pre'});
  document.body.append(e3, e2, e1, e4, e5);

  // 星图数据 —— 从 frontend/components/StarChart.tsx 原样抽出，未做任何改动。
//   SKY   : 88 个 IAU 星座的连线数据，[[ra,dec], …] 段
//   STARS : 519 颗亮星 [RA*100, Dec*100, mag*100, "r,g,b"]（B-V 真实色）
// 经典 script（非 module）—— 这样 file:// 直接打开也能加载。
window.SKY = {"And":[[[30.97,42.33],[17.43,35.62],[9.83,30.86],[2.1,29.09]],[[14.3,23.42],[11.83,24.27],[9.64,29.31],[9.83,30.86],[9.22,33.72],[-5.47,43.27],[-14.52,42.33]],[[-5.47,43.27],[-4.9,44.33],[-5.61,46.46]],[[17.43,35.62],[14.19,38.5],[12.45,41.08],[17.38,47.24],[24.5,48.63]],[[-4.9,44.33],[-3.49,46.42]]],"Ant":[[[142.31,-35.95],[156.79,-31.07],[164.18,-37.14]]],"Aps":[[[-138.03,-79.04],[-114.91,-78.7],[-109.23,-77.52],[-111.64,-78.9]]],"Aqr":[[[-48.08,-9.5],[-46.84,-8.98],[-37.11,-5.57],[-28.55,-0.32],[-24.59,-1.39],[-22.79,-0.02],[-21.16,-0.12],[-16.85,-7.58],[-10.52,-9.18],[-12.64,-21.17]],[[-37.11,-5.57],[-28.39,-13.87]],[[-28.55,-0.32],[-25.79,-7.78]],[[-22.79,-0.02],[-23.68,1.38]],[[-9.26,-20.1],[-10.52,-9.18],[-4.56,-17.82]]],"Aql":[[[-63.44,10.61],[-62.3,8.87],[-61.17,6.41],[-57.17,-0.82],[-61.88,1.01],[-68.63,3.11],[-73.65,13.86],[-62.3,8.87],[-68.63,3.11],[-73.44,-4.88]]],"Ara":[[[-98.65,-56.38],[-97.23,-60.68],[-107.55,-59.04],[-105.34,-55.99],[-105.1,-53.16],[-97.04,-49.88],[-98.67,-55.53]]],"Ari":[[[42.5,27.26],[31.79,23.46],[28.66,20.81],[28.38,19.29]]],"Aur":[[[89.88,44.95],[79.17,46.0],[76.63,41.23],[74.25,33.17],[81.57,28.61],[89.93,37.21],[89.88,44.95],[89.88,54.28],[79.17,46.0],[75.49,43.82],[75.62,41.08]]],"Boo":[[[-153.18,17.46],[-151.33,18.4],[-146.08,19.18],[-142.04,30.37],[-141.98,38.31],[-134.51,40.39],[-131.12,33.31],[-138.75,27.07],[-146.08,19.18],[-139.71,13.73]],[[-141.98,38.31],[-145.9,46.09],[-146.63,51.79],[-143.7,51.85],[-145.9,46.09]]],"Cae":[[[67.71,-44.95],[70.14,-41.86],[70.51,-37.14],[76.1,-35.48]]],"Cam":[[[74.32,53.75],[75.85,60.44],[73.51,66.34],[57.59,71.33],[57.38,65.53],[52.27,59.94]],[[73.51,66.34],[94.71,69.32],[105.02,76.98]]],"Cnc":[[[134.62,11.86],[131.17,18.15],[130.82,21.47],[131.67,28.77]],[[131.17,18.15],[124.13,9.19]]],"CVn":[[[-166.0,38.31],[-171.56,41.36]]],"CMa":[[[95.67,-17.96],[101.29,-16.72],[105.76,-23.83],[107.1,-26.39],[105.43,-27.93],[104.66,-28.97],[95.08,-30.06]],[[111.02,-29.3],[107.1,-26.39]],[[101.29,-16.72],[104.03,-17.05],[105.94,-15.63],[103.55,-12.04],[104.03,-17.05]]],"CMi":[[[114.83,5.22],[111.79,8.29]]],"Cap":[[[-55.59,-12.51],[-54.75,-14.78],[-52.78,-17.81],[-48.48,-25.27],[-47.04,-26.92],[-38.33,-22.41],[-33.24,-16.13],[-34.98,-16.66],[-39.44,-16.83],[-43.51,-17.23],[-55.59,-12.51]]],"Car":[[[99.44,-43.2],[95.99,-52.7],[138.3,-69.72],[153.43,-70.04],[160.74,-64.39],[158.01,-61.69],[154.27,-61.33],[139.27,-59.28],[125.63,-59.51],[119.19,-52.98],[122.38,-47.34],[131.18,-54.71],[139.27,-59.28]],[[160.74,-64.39],[166.64,-62.42],[167.14,-61.95],[168.15,-60.32],[167.15,-58.98],[163.37,-58.85],[158.01,-61.69]]],"Cas":[[[28.6,63.67],[21.45,60.24],[14.18,60.72],[10.13,56.54],[2.29,59.15]]],"Cen":[[[170.25,-54.49],[-177.91,-50.72],[-172.99,-50.23],[-169.62,-48.96],[-155.03,-53.47],[-151.12,-47.29],[-152.6,-42.47],[-152.62,-41.69],[-148.33,-36.37],[-141.12,-42.16],[-135.21,-42.1]],[[-152.62,-41.69],[-159.85,-36.71]],[[-140.1,-60.84],[-155.03,-53.47],[-149.04,-60.37]],[[-172.99,-50.23],[-177.09,-52.37],[172.94,-59.44]]],"Cep":[[[-52.6,62.99],[-48.68,61.84],[-40.36,62.59],[-34.12,58.78],[-26.24,57.04],[-27.29,58.2],[-22.71,58.42],[-17.58,66.2],[-5.16,77.63],[-37.84,70.56],[-40.36,62.59]],[[-37.84,70.56],[-17.58,66.2]]],"Cet":[[[40.83,3.24],[38.97,5.59],[37.04,8.46],[41.24,10.11],[44.93,8.91],[45.57,4.09],[40.83,3.24],[39.87,0.33],[34.84,-2.98],[27.87,-10.34],[26.02,-15.94],[10.9,-17.99],[4.86,-8.82],[17.15,-10.18],[21.01,-8.18],[27.87,-10.34]]],"Cha":[[[124.63,-76.92],[158.87,-78.61],[161.32,-80.47],[-175.41,-79.31],[179.91,-78.22],[158.87,-78.61]]],"Cir":[[[-130.62,-58.8],[-139.37,-64.98],[-129.16,-59.32]]],"Col":[[[95.53,-33.44],[87.74,-35.77],[84.91,-34.07],[82.8,-35.47]],[[87.74,-35.77],[89.79,-42.82]]],"Com":[[[-162.5,17.53],[-162.03,27.88],[-173.27,28.27]]],"CrA":[[[-75.32,-37.11],[-73.4,-37.06],[-72.63,-37.9],[-72.49,-39.34],[-72.91,-40.5],[-74.22,-42.1],[-77.6,-43.43],[-81.62,-42.31]]],"CrB":[[[-126.77,31.36],[-128.04,29.11],[-126.33,26.71],[-124.31,26.3],[-122.6,26.07],[-120.6,26.88],[-119.64,29.85]]],"Crv":[[[-177.9,-24.73],[-177.47,-22.62],[-176.05,-17.54],[-172.53,-16.52],[-171.4,-23.4],[-177.47,-22.62]]],"Crt":[[[174.17,-9.8],[171.15,-10.86],[169.84,-14.78],[164.94,-18.3],[167.91,-22.83],[170.84,-18.78],[171.22,-17.68],[176.19,-18.35],[179.0,-17.15]],[[169.84,-14.78],[171.22,-17.68]]],"Cru":[[[-168.07,-59.69],[-176.21,-58.75]],[[-173.35,-63.1],[-172.21,-57.11]]],"Cyg":[[[-41.77,30.23],[-48.45,33.97],[-54.44,40.26],[-63.76,45.13],[-67.57,51.73],[-70.72,53.37]],[[-49.64,45.28],[-54.44,40.26],[-60.92,35.08],[-67.32,27.96]]],"Del":[[[-51.7,11.3],[-50.61,14.6],[-50.09,15.91],[-48.34,16.12],[-49.14,15.07],[-50.61,14.6]]],"Dor":[[[64.01,-51.49],[68.5,-55.05],[83.41,-62.49],[86.19,-65.74],[88.53,-63.09],[83.41,-62.49],[76.38,-57.47],[68.5,-55.05]]],"Dra":[[[-91.62,56.87],[-90.85,51.49],[-97.39,52.3],[-96.93,55.17],[-91.62,56.87],[-71.86,67.66],[-84.81,71.34],[-102.8,65.71],[-114.0,61.51],[-119.53,58.57],[-128.77,58.97],[-148.9,64.38],[-171.63,69.79],[172.85,69.33]],[[-84.81,71.34],[-84.74,72.73]],[[-71.86,67.66],[-62.96,70.27]]],"Equ":[[[-41.04,5.25],[-41.38,10.01],[-42.41,10.13]]],"Eri":[[[76.96,-5.09],[71.38,-3.25],[69.08,-3.35],[62.97,-6.84],[59.51,-13.51],[56.54,-12.1],[55.81,-9.76],[53.23,-9.46],[44.11,-8.9],[41.03,-13.86],[41.28,-18.57],[45.6,-23.62],[49.88,-21.76],[53.45,-21.63],[56.71,-23.25],[68.89,-30.56],[66.01,-34.02],[64.47,-33.8],[57.36,-36.2],[54.27,-40.27],[49.98,-43.07],[44.57,-40.3],[40.17,-39.86],[36.75,-47.7],[34.13,-51.51],[28.99,-51.61],[24.43,-57.24]]],"For":[[[48.02,-28.99],[42.27,-32.41],[31.12,-29.3]]],"Gem":[[[93.72,22.51],[95.74,22.51],[100.98,25.13],[107.78,30.25],[113.65,31.89],[116.33,28.03],[113.98,26.9],[110.03,21.98],[106.03,20.57],[99.43,16.4],[101.32,12.9]],[[110.03,21.98],[109.52,16.54]]],"Gru":[[[-14.78,-52.75],[-17.86,-51.32],[-19.33,-46.88],[-22.56,-43.75],[-27.94,-46.96],[-19.33,-46.88]],[[-22.68,-43.5],[-26.1,-41.35],[-28.47,-39.54],[-31.52,-37.36]]],"Her":[[[-114.52,19.15],[-112.44,21.49],[-109.68,31.6],[-109.28,38.92],[-111.47,42.44],[-115.06,46.31],[-117.81,44.93],[-121.83,42.45]],[[-109.68,31.6],[-104.93,30.93]],[[-109.28,38.92],[-101.24,36.81]],[[-90.94,37.25],[-99.08,37.15],[-101.24,36.81],[-104.93,30.93],[-101.24,24.84],[-93.39,27.72],[-90.56,29.25],[-88.11,28.76]],[[-101.34,14.39],[-112.44,21.49]]],"Hor":[[[63.5,-42.29],[40.64,-50.8],[39.35,-52.54],[40.17,-54.55],[45.9,-59.74],[44.7,-64.07]]],"Hya":[[[131.69,6.42],[132.11,5.84],[130.81,3.4],[129.69,3.34],[129.41,5.7],[131.69,6.42],[133.85,5.95],[138.59,2.31],[144.96,-1.14],[141.9,-8.66],[147.87,-14.85],[152.65,-12.35],[156.52,-16.84],[162.41,-16.19],[173.25,-31.86],[178.23,-33.91],[-160.27,-23.17],[-148.41,-26.68],[-137.43,-27.96]]],"Hyi":[[[6.44,-77.25],[56.81,-74.24],[39.9,-68.27],[35.44,-68.66],[28.73,-67.65],[29.69,-61.57]]],"Ind":[[[-50.61,-47.29],[-48.99,-51.92],[-46.3,-58.45],[-30.52,-54.99],[-40.03,-53.45],[-50.61,-47.29]]],"Lac":[[[-24.11,52.23],[-22.18,50.28],[-22.62,47.71],[-24.74,46.54],[-22.38,43.12],[-19.87,44.28],[-22.62,47.71],[-23.87,49.48],[-24.11,52.23]],[[-22.38,43.12],[-26.53,39.71],[-26.01,37.75]]],"Leo":[[[152.09,11.97],[151.83,16.76],[154.99,19.84],[168.53,20.52],[177.26,14.57],[168.56,15.43],[152.09,11.97]],[[154.99,19.84],[154.17,23.42],[148.19,26.01],[146.46,23.77]]],"LMi":[[[151.86,35.24],[156.48,33.8],[163.33,34.21],[156.97,36.71],[151.86,35.24],[143.56,36.4]]],"Lep":[[[91.54,-14.94],[89.1,-14.17],[86.74,-14.82],[83.18,-17.82],[78.23,-16.21],[76.37,-22.37],[82.06,-20.76],[86.12,-22.45],[87.83,-20.88]],[[78.31,-12.94],[78.23,-16.21],[79.89,-13.18]]],"Lib":[[[-133.98,-25.28],[-137.28,-16.04],[-130.75,-9.38],[-126.12,-14.79],[-125.74,-28.14],[-125.34,-29.78]],[[-137.28,-16.04],[-126.12,-14.79]]],"Lup":[[[-122.26,-33.63],[-125.06,-34.41],[-129.55,-36.26],[-129.66,-40.65],[-135.37,-43.13],[-139.52,-47.39],[-131.93,-52.1],[-130.37,-47.88],[-129.33,-44.69],[-126.21,-41.17],[-119.97,-38.4],[-118.35,-36.8]],[[-129.66,-40.65],[-126.21,-41.17]]],"Lyn":[[[94.91,59.01],[104.32,58.42],[111.68,49.21],[125.71,43.19],[135.16,41.78],[139.71,36.8],[140.26,34.39]]],"Lyr":[[[-78.81,37.61],[-78.91,39.61],[-80.77,38.78],[-78.81,37.61],[-76.37,36.9],[-75.26,32.69],[-77.48,33.36],[-78.81,37.61]]],"Men":[[[92.56,-74.75],[82.97,-76.34],[73.8,-74.94],[75.68,-71.31]]],"Mic":[[[-47.51,-33.78],[-47.88,-43.99],[-39.81,-40.81],[-40.52,-32.17],[-44.68,-32.26],[-47.51,-33.78]]],"Mon":[[[115.31,-9.55],[122.15,-2.98],[107.97,-0.49],[97.2,-7.03],[93.71,-6.27]],[[107.97,-0.49],[101.97,2.41],[95.94,4.59],[98.23,7.33],[100.24,9.9]]],"Mus":[[[176.4,-66.73],[-175.61,-67.96],[-170.7,-69.14],[-168.43,-68.11],[-164.43,-71.55],[-171.88,-72.13],[-170.7,-69.14]]],"Nor":[[[-118.38,-45.17],[-113.2,-47.55],[-115.04,-50.16],[-119.2,-49.23],[-118.38,-45.17]]],"Oct":[[[-143.27,-83.67],[-18.49,-81.38],[-34.63,-77.39],[-143.27,-83.67]]],"Oph":[[[-90.24,-9.77],[-93.03,2.71],[-94.13,4.57],[-96.27,12.56],[-105.58,9.38],[-112.27,1.98],[-116.41,-3.69],[-115.42,-4.69],[-110.71,-10.57],[-102.41,-15.72]],[[-105.58,9.38],[-110.71,-10.57],[-112.22,-16.61],[-113.24,-18.46],[-113.97,-20.04],[-113.6,-23.45]],[[-94.13,4.57],[-102.41,-15.72],[-99.5,-25.0],[-98.16,-29.87]]],"Ori":[[[91.89,14.77],[88.6,20.28],[90.98,20.14],[92.98,14.21],[90.6,9.65],[88.79,7.41],[81.28,6.35],[73.72,10.15]],[[74.64,1.71],[73.56,2.44],[72.8,5.61],[72.46,6.96],[72.65,8.9],[73.72,10.15],[74.09,13.51],[76.14,15.4],[77.42,15.6]],[[78.63,-8.2],[81.12,-2.4],[83.0,-0.3],[81.28,6.35],[83.78,9.93],[88.79,7.41],[85.19,-1.94],[86.94,-9.67]],[[85.19,-1.94],[84.05,-1.2],[83.0,-0.3]]],"Pav":[[[-53.59,-56.74],[-48.76,-66.2],[-57.82,-66.18],[-76.95,-62.19],[-84.19,-61.49],[-87.85,-63.67],[-93.57,-64.72],[-79.24,-71.43],[-59.85,-72.91],[-48.76,-66.2],[-38.39,-65.37]]],"Peg":[[[-27.5,33.18],[-19.25,30.22],[-14.06,28.08],[2.1,29.09],[3.31,15.18],[-13.81,15.21],[-18.33,12.17],[-19.63,10.83],[-27.45,6.2],[-33.95,9.88]],[[-13.81,15.21],[-14.06,28.08],[-17.5,24.6],[-18.37,23.57],[-28.25,25.35],[-33.84,25.64]]],"Per":[[[56.08,32.29],[58.53,31.88],[59.74,35.79],[59.46,40.01],[56.3,42.58],[55.73,47.79],[54.12,48.19],[51.08,49.86],[46.2,53.51],[42.67,55.9],[43.56,52.76],[47.27,49.61],[47.37,44.86],[47.04,40.96],[47.82,39.61],[46.29,38.84],[44.69,39.66],[44.92,41.03],[47.04,40.96]],[[61.65,50.35],[63.72,48.41],[62.17,47.71],[55.73,47.79]],[[47.27,49.61],[41.05,49.23],[25.92,50.69]]],"Phe":[[[6.57,-42.31],[16.52,-46.72],[22.09,-43.32],[22.81,-49.07],[17.1,-55.25],[16.52,-46.72],[2.35,-45.75],[6.57,-42.31]]],"Pic":[[[102.05,-61.94],[87.46,-56.17],[86.82,-51.07]]],"Psc":[[[18.44,24.58],[17.92,30.09],[19.87,27.26],[18.44,24.58],[17.86,21.03],[22.87,15.35],[26.35,9.16],[30.51,2.76],[28.39,3.19],[25.36,5.49],[22.55,6.14],[18.43,7.58],[15.74,7.89],[12.17,7.59],[-0.17,6.86],[-5.01,5.63],[-8.01,6.38],[-9.91,5.38],[-10.71,3.28],[-8.27,1.26],[-4.49,1.78],[-3.4,3.49],[-5.01,5.63]],[[-10.71,3.28],[-14.03,3.82]]],"PsA":[[[-19.84,-27.04],[-15.59,-29.62],[-16.01,-32.54],[-16.87,-32.88],[-22.12,-32.35],[-27.9,-32.99],[-33.76,-33.03],[-33.07,-30.9],[-27.9,-32.99],[-19.84,-27.04]]],"Pup":[[[99.44,-43.2],[109.29,-37.1],[113.85,-28.37],[114.71,-26.8],[117.32,-24.86],[119.21,-22.88],[121.89,-24.3],[120.9,-40.0],[122.38,-47.34]],[[117.32,-24.86],[117.02,-25.94],[115.95,-28.95],[113.85,-28.37]]],"Pyx":[[[120.9,-40.0],[130.03,-35.31],[130.9,-33.19],[132.63,-27.71]]],"Ret":[[[63.61,-62.47],[64.12,-59.3],[59.69,-61.4],[56.05,-64.81],[63.61,-62.47]]],"Sge":[[[-64.98,18.01],[-63.15,18.53],[-60.31,19.49]],[[-64.74,17.48],[-63.15,18.53]]],"Sgr":[[[-85.59,-36.76],[-83.96,-34.38],[-84.75,-29.83],[-83.01,-25.42],[-86.56,-21.06]],[[-69.34,-44.46],[-69.03,-40.62],[-74.35,-29.88],[-78.59,-26.99],[-83.01,-25.42]],[[-61.18,-41.87],[-60.07,-35.28],[-61.04,-26.3],[-65.82,-24.88],[-68.68,-24.51],[-71.11,-25.26],[-76.18,-26.3],[-78.59,-26.99],[-84.75,-29.83],[-88.55,-30.42],[-83.96,-34.38],[-74.35,-29.88],[-73.27,-27.67],[-76.18,-26.3],[-73.83,-21.74],[-72.56,-21.02],[-70.59,-18.95],[-69.58,-17.85],[-69.57,-15.96]],[[-73.83,-21.74],[-75.57,-21.11],[-76.46,-22.74],[-76.18,-26.3]]],"Sco":[[[-120.29,-26.11],[-119.92,-22.62],[-118.64,-19.81]],[[-119.92,-22.62],[-114.7,-25.59],[-112.65,-26.43],[-111.03,-28.22],[-107.46,-34.29],[-107.03,-38.05],[-106.35,-42.36],[-101.96,-43.24],[-95.67,-43.0],[-93.1,-40.13],[-94.38,-39.03],[-96.6,-37.1]]],"Scl":[[[14.65,-29.36],[-2.77,-28.13],[-10.29,-32.53],[-6.76,-37.82]]],"Sct":[[[-81.2,-8.24],[-78.21,-4.75],[-79.43,-9.05],[-82.7,-14.57],[-81.2,-8.24]]],"Ser":[[[-102.41,-15.72],[-95.6,-15.4],[-90.24,-9.77],[-89.23,-8.18],[-84.67,-2.9],[-75.95,4.2]]],"Sex":[[[151.98,-0.37],[148.13,-8.11],[157.37,-2.74],[157.57,-0.64]]],"Tau":[[[84.41,21.14],[68.98,16.51],[67.17,15.87],[64.95,15.63],[65.73,17.54],[67.15,19.18],[81.57,28.61]],[[64.95,15.63],[60.17,12.49],[51.79,9.73],[60.79,5.99]],[[51.79,9.73],[51.2,9.03],[54.22,0.4]]],"Tel":[[[-87.19,-45.95],[-83.26,-45.97],[-82.79,-49.07]]],"Tri":[[[28.27,29.58],[32.39,34.99],[34.33,33.85],[28.27,29.58]]],"TrA":[[[-107.83,-69.03],[-121.21,-63.43],[-130.27,-68.68],[-107.83,-69.03]]],"Tuc":[[[-25.37,-60.26],[-10.64,-58.24],[7.89,-62.96],[5.02,-64.87],[-0.02,-65.58],[-23.17,-64.97],[-25.37,-60.26]]],"UMa":[[[-176.14,57.03],[165.93,61.75],[165.46,56.38],[178.46,53.69],[-176.14,57.03],[-166.49,55.96],[-159.02,54.93],[-153.11,49.31]],[[178.46,53.69],[176.51,47.78],[169.62,33.09],[169.55,31.53]],[[176.51,47.78],[167.42,44.5],[155.58,41.5]],[[167.42,44.5],[154.27,42.91]],[[165.93,61.75],[142.88,63.06],[127.57,60.72],[147.75,59.04],[165.46,56.38]],[[165.46,56.38],[148.03,54.06],[143.21,51.68],[134.8,48.04]],[[135.91,47.16],[143.21,51.68]]],"UMi":[[[-123.99,77.79],[-115.62,75.76],[-129.82,71.83],[-137.32,74.16],[-123.99,77.79],[-108.51,82.04],[-96.95,86.59],[37.95,89.26]]],"Vel":[[[131.18,-54.71],[140.53,-55.01],[149.22,-54.57],[161.69,-49.42],[153.68,-42.12],[142.68,-40.47],[137.0,-43.43],[122.38,-47.34]]],"Vir":[[[176.46,6.53],[177.67,1.76],[-175.02,-0.67],[-169.58,-1.45],[-162.51,-5.54],[-158.7,-11.16],[-146.0,-6.0],[-139.23,-5.66]],[[-164.46,10.96],[-166.1,3.4],[-169.58,-1.45]],[[-162.51,-5.54],[-156.33,-0.6],[-149.59,1.54],[-138.44,1.89]]],"Vol":[[[135.61,-66.4],[126.43,-66.14],[121.98,-68.62],[109.21,-67.96],[107.19,-70.5],[121.98,-68.62],[135.61,-66.4]]],"Vul":[[[-70.95,21.39],[-67.82,24.66],[-61.63,24.08],[-59.72,27.75],[-56.06,27.81]]]};
window.STARS = [[210,2909,207,"201,215,255"],[229,5915,228,"252,248,242"],[235,-4575,388,"255,234,191"],[331,1518,283,"177,194,255"],[486,-882,356,"255,229,174"],[644,-7725,282,"255,244,224"],[655,-4368,393,"230,234,245"],[657,-4231,240,"255,232,185"],[924,5390,369,"176,194,255"],[983,3086,327,"255,227,170"],[1013,5654,224,"255,230,178"],[1090,-1799,204,"255,234,190"],[1228,5782,346,"255,245,226"],[1418,6072,215,"200,214,255"],[1419,3850,386,"225,230,245"],[1652,-4672,332,"255,237,202"],[1710,-5525,394,"187,203,255"],[1715,-1018,346,"255,230,179"],[1743,3562,207,"255,219,144"],[2101,-818,360,"255,232,187"],[2145,6024,266,"228,233,245"],[2209,-4332,341,"255,220,147"],[2281,-4907,393,"255,235,194"],[2287,1535,362,"255,235,194"],[2443,-5724,45,"181,198,255"],[2450,4863,359,"255,227,169"],[2602,-1594,349,"255,241,215"],[2787,-1034,374,"255,231,181"],[2827,2958,342,"255,247,234"],[2838,1929,388,"200,214,255"],[2860,6367,335,"183,199,255"],[2866,2081,264,"229,233,245"],[2899,-5161,369,"255,238,205"],[2969,-6157,286,"243,242,243"],[3000,-2108,399,"255,220,146"],[3051,276,382,"214,223,247"],[3086,7242,395,"208,221,255"],[3097,4233,210,"255,225,161"],[3179,2346,201,"255,230,180"],[3239,3499,300,"226,231,245"],[3413,-5151,356,"187,203,255"],[3795,8926,197,"255,243,222"],[4083,324,347,"221,228,246"],[4250,2726,361,"190,206,255"],[4267,5590,377,"255,217,135"],[4356,5276,393,"255,240,212"],[4411,-890,389,"255,232,185"],[4457,-4030,288,"225,230,245"],[4557,409,254,"255,218,140"],[4620,5351,291,"255,241,216"],[4629,3884,332,"255,221,148"],[4704,4096,209,"208,221,255"],[4737,4486,379,"255,235,194"],[4802,-2899,380,"255,246,230"],[4988,-2176,370,"255,218,141"],[5108,4986,179,"255,247,235"],[5120,903,361,"255,237,201"],[5179,973,373,"193,208,255"],[5323,-946,372,"255,237,202"],[5573,4779,301,"186,202,255"],[5581,-976,352,"255,236,199"],[5605,-6481,384,"255,231,181"],[5608,3229,384,"214,223,247"],[5622,2411,372,"190,205,255"],[5630,4258,377,"255,249,240"],[5646,2437,387,"197,211,255"],[5681,-7424,326,"255,219,143"],[5687,2411,285,"193,208,255"],[5729,2405,362,"196,210,255"],[5853,3188,284,"241,240,243"],[5946,4001,290,"176,193,255"],[5951,-1351,297,"255,219,143"],[5974,3579,398,"213,222,247"],[6017,1249,341,"191,206,255"],[6079,599,391,"215,224,246"],[6217,4771,396,"204,217,255"],[6350,-4229,385,"255,232,185"],[6361,-6247,333,"255,236,199"],[6447,-3380,355,"189,205,255"],[6495,1563,365,"255,235,194"],[6573,1754,377,"255,235,193"],[6601,-3402,397,"255,222,153"],[6714,1596,384,"255,235,196"],[6715,1918,353,"255,234,191"],[6717,1587,340,"231,234,245"],[6850,-5504,330,"194,209,255"],[6889,-3056,381,"255,235,196"],[6898,1651,87,"255,220,147"],[6908,-335,393,"174,192,255"],[6955,-1430,386,"255,232,185"],[7246,696,319,"255,247,235"],[7280,561,368,"182,198,255"],[7356,244,371,"178,196,255"],[7425,3317,269,"255,222,151"],[7549,4382,303,"255,246,230"],[7562,4108,369,"255,230,179"],[7637,-2237,319,"255,222,154"],[7663,4123,318,"183,199,255"],[7696,-509,278,"229,233,245"],[7823,-1621,329,"189,204,255"],[7863,-820,18,"203,216,255"],[7917,4600,8,"255,239,209"],[7940,-684,359,"188,204,255"],[8112,-240,335,"170,189,255"],[8128,635,164,"172,191,255"],[8157,2861,165,"186,202,255"],[8206,-2076,281,"255,239,208"],[8280,-3547,386,"255,231,181"],[8300,-30,225,"179,196,255"],[8318,-1782,258,"234,236,244"],[8341,-6249,376,"255,243,222"],[8378,993,339,"181,198,255"],[8386,-591,275,"174,192,255"],[8405,-120,169,"178,195,255"],[8441,2114,297,"183,199,255"],[8469,-260,377,"177,194,255"],[8491,-3407,265,"187,203,255"],[8519,-194,174,"176,193,255"],[8612,-2245,359,"255,247,235"],[8674,-1482,355,"222,229,246"],[8682,-5107,385,"230,233,245"],[8694,-967,207,"180,197,255"],[8774,-3577,312,"255,230,180"],[8783,-2088,376,"255,235,193"],[8787,3915,397,"255,231,181"],[8879,741,45,"255,221,151"],[8910,-1417,371,"248,245,243"],[8979,-4282,396,"255,230,180"],[8988,5428,372,"255,234,191"],[8988,4495,190,"219,227,246"],[8993,3721,265,"193,208,255"],[9371,-627,399,"255,226,166"],[9372,2251,331,"255,219,142"],[9508,-3006,302,"181,198,255"],[9553,-3344,385,"255,238,204"],[9567,-1796,198,"170,189,255"],[9574,2251,287,"255,218,141"],[9599,-5270,-62,"229,233,245"],[9720,-703,376,"188,204,255"],[9917,-1926,395,"255,233,189"],[9943,1640,193,"211,221,247"],[9944,-4320,317,"190,205,255"],[10098,2513,306,"255,224,161"],[10129,-1672,-144,"212,222,247"],[10132,1290,335,"255,248,238"],[10205,-6194,324,"236,237,244"],[10246,-3251,350,"188,204,255"],[10248,-5061,294,"255,229,175"],[10320,3396,360,"222,229,246"],[10353,-2418,389,"255,215,131"],[10466,-2897,150,"174,192,255"],[10543,-2793,349,"255,216,132"],[10576,-2383,302,"194,209,255"],[10710,-2639,183,"255,242,219"],[10719,-7050,378,"255,234,192"],[10921,-6796,397,"255,240,212"],[10929,-3710,271,"255,218,141"],[10952,1654,358,"223,229,245"],[11003,2198,350,"252,248,242"],[11102,-2930,245,"193,208,255"],[11143,2780,378,"255,233,190"],[11179,829,289,"191,206,255"],[11231,-4330,325,"255,221,150"],[11365,3189,158,"215,224,246"],[11471,-2680,380,"181,198,255"],[11483,522,40,"255,249,239"],[11531,-955,394,"255,234,190"],[11546,-7261,393,"255,233,189"],[11595,-2895,394,"228,233,245"],[11611,2440,357,"255,236,198"],[11631,-3797,362,"255,216,134"],[11633,2803,116,"255,234,193"],[11732,-2486,334,"255,229,174"],[11805,-4058,371,"255,234,191"],[11919,-5298,346,"179,196,255"],[12090,-4000,221,"167,187,255"],[12189,-2430,283,"255,248,237"],[12238,-4734,175,"183,200,255"],[12413,919,353,"255,222,152"],[12563,-5951,186,"255,229,176"],[12642,-391,391,"206,219,255"],[12643,-6614,377,"255,231,181"],[12757,6072,335,"255,238,204"],[13003,-3531,397,"255,236,197"],[13007,-5292,360,"180,197,255"],[13016,-4665,377,"255,243,219"],[13090,-3319,368,"178,196,255"],[13117,1815,394,"255,232,185"],[13118,-5471,193,"216,224,246"],[13151,-4604,387,"213,222,247"],[13169,642,338,"255,242,218"],[13376,-6064,384,"190,205,255"],[13385,595,311,"255,235,194"],[13480,4804,312,"235,237,244"],[13516,4178,396,"255,248,237"],[13561,-6640,400,"227,232,245"],[13591,4716,357,"212,222,247"],[13604,-4710,375,"255,230,178"],[13700,-4343,223,"255,217,137"],[13774,-5897,343,"177,194,255"],[13782,-6232,396,"178,196,255"],[13830,-6972,167,"219,226,246"],[13859,231,389,"197,212,255"],[13927,-5928,221,"232,235,244"],[13971,3680,382,"218,226,246"],[14026,3439,314,"255,220,146"],[14053,-5501,247,"184,200,255"],[14190,-866,199,"255,223,156"],[14268,-4047,360,"251,247,242"],[14281,-5703,316,"255,220,147"],[14288,6306,365,"250,247,242"],[14321,5168,317,"255,247,236"],[14496,-114,390,"255,226,166"],[14529,989,352,"255,246,232"],[14631,-6251,369,"255,234,191"],[14646,2377,297,"255,239,208"],[14678,-6507,292,"241,240,243"],[14775,5904,378,"243,242,243"],[14819,2601,388,"255,228,174"],[14922,-5457,352,"196,211,255"],[15183,1676,348,"203,216,255"],[15209,1197,136,"193,208,255"],[15265,-1235,361,"255,234,191"],[15343,-7004,329,"195,209,255"],[15368,-4212,385,"217,225,246"],[15417,2342,343,"244,243,243"],[15427,-6133,339,"255,220,147"],[15427,4291,345,"214,223,246"],[15499,1984,201,"255,231,181"],[15558,4150,306,"255,219,142"],[15610,-7403,399,"251,247,242"],[15652,-1684,383,"255,222,154"],[15697,-5874,381,"246,244,243"],[15801,-6169,330,"192,207,255"],[15820,931,384,"183,199,255"],[15933,-4823,384,"244,242,243"],[16074,-6439,274,"173,191,255"],[16169,-4942,269,"255,237,200"],[16241,-1619,311,"255,228,173"],[16333,3421,379,"255,233,189"],[16337,-5885,378,"255,236,197"],[16546,5638,234,"215,224,246"],[16593,6175,181,"255,233,187"],[16715,-5898,393,"255,228,173"],[16742,4450,300,"255,230,180"],[16853,2052,256,"225,230,245"],[16856,1543,333,"208,221,255"],[16955,3153,379,"255,244,225"],[16962,3309,349,"255,224,159"],[16984,-1478,356,"255,231,183"],[17025,-5449,390,"182,198,255"],[17098,1053,400,"255,249,240"],[17285,6933,382,"255,218,141"],[17325,-3186,354,"255,235,196"],[17395,-6302,311,"200,214,255"],[17640,-6673,363,"228,233,245"],[17651,4778,369,"255,229,177"],[17726,1457,214,"221,228,246"],[17767,176,359,"255,246,232"],[17846,5369,241,"216,224,246"],[-17791,-5072,258,"186,202,255"],[-17747,-2262,302,"255,226,165"],[-17709,-5237,397,"182,198,255"],[-17621,-5875,279,"176,194,255"],[-17614,5703,332,"219,227,246"],[-17605,-1754,258,"189,205,255"],[-17502,-67,389,"214,223,247"],[-17466,-6040,359,"255,224,160"],[-17335,-6310,77,"170,189,255"],[-17299,-5023,391,"177,194,255"],[-17253,-1652,294,"206,219,255"],[-17221,-5711,159,"255,219,142"],[-17188,-7213,384,"182,198,255"],[-17163,6979,385,"188,204,255"],[-17140,-2340,265,"255,237,201"],[-17070,-6914,269,"179,196,255"],[-17057,-4854,385,"216,225,246"],[-16962,-4896,220,"204,217,255"],[-16958,-145,274,"251,247,242"],[-16843,-6811,304,"178,196,255"],[-16807,-5969,125,"171,189,255"],[-16649,5596,176,"204,218,255"],[-16610,340,339,"255,220,145"],[-16599,3832,289,"188,204,255"],[-16446,1096,285,"255,236,197"],[-16443,-7155,361,"255,229,176"],[-16027,-2317,299,"255,236,199"],[-15985,-3671,275,"219,226,246"],[-15902,5493,223,"217,225,246"],[-15870,-1116,98,"171,190,255"],[-15869,5499,399,"229,233,245"],[-15724,-3941,390,"255,229,177"],[-15633,-60,338,"224,229,245"],[-15503,-5347,229,"179,197,255"],[-15311,4931,185,"191,206,255"],[-15262,-4169,341,"172,191,255"],[-15260,-4247,347,"180,197,255"],[-15133,1840,268,"255,245,227"],[-15112,-4729,255,"179,196,255"],[-15043,-4210,383,"172,191,255"],[-15033,-4480,387,"174,192,255"],[-14904,-6037,61,"171,190,255"],[-14890,6438,367,"199,213,255"],[-14841,-2668,325,"255,232,184"],[-14833,-3637,206,"255,234,191"],[-14608,1918,-5,"255,228,172"],[-14515,-4606,355,"178,195,255"],[-14204,3037,357,"255,227,167"],[-14198,3831,304,"232,235,244"],[-14112,-4216,233,"182,198,255"],[-14010,-6084,135,"255,237,200"],[-14010,-6083,-1,"255,241,216"],[-13971,1373,378,"216,224,246"],[-13952,-4739,230,"182,199,255"],[-13937,-6498,318,"239,239,244"],[-13923,-566,387,"253,248,242"],[-13875,2707,235,"255,235,195"],[-13844,189,373,"208,220,255"],[-13803,-7904,383,"255,223,156"],[-13732,7416,207,"255,222,153"],[-13728,-1604,275,"227,232,245"],[-13537,-4313,268,"178,195,255"],[-13521,-4210,313,"174,192,255"],[-13451,4039,349,"255,235,196"],[-13398,-2528,325,"255,217,136"],[-13372,-4705,391,"183,200,255"],[-13202,-4874,388,"203,216,255"],[-13193,-5210,341,"255,236,199"],[-13112,3331,346,"255,235,195"],[-13075,-938,261,"195,210,255"],[-13027,-6868,287,"213,222,247"],[-12982,7183,300,"217,225,246"],[-12966,-4065,322,"172,190,255"],[-12955,-3626,357,"255,220,148"],[-12933,-4469,337,"177,194,255"],[-12877,5897,329,"255,230,178"],[-12804,2911,366,"246,244,243"],[-12633,2671,222,"215,224,246"],[-12630,1054,380,"240,240,243"],[-12621,-4117,280,"173,192,255"],[-12612,-1479,391,"255,234,191"],[-12574,-2814,360,"255,225,162"],[-12534,-2978,366,"179,196,255"],[-12431,2630,381,"213,223,247"],[-12393,643,263,"255,230,178"],[-12345,1542,365,"219,226,246"],[-12260,-343,354,"202,215,255"],[-12230,448,371,"227,232,245"],[-12226,-3363,397,"200,214,255"],[-12121,-6343,283,"245,243,243"],[-12089,1566,385,"255,247,235"],[-12078,-2921,387,"176,193,255"],[-12029,-2611,289,"178,196,255"],[-11997,-3840,342,"175,193,255"],[-11992,-2262,229,"188,203,255"],[-11864,-1981,256,"196,211,255"],[-11830,-2067,393,"200,214,255"],[-11700,-1946,400,"219,227,246"],[-11641,-369,273,"255,219,144"],[-11614,-6369,386,"255,231,183"],[-11542,-469,323,"255,235,195"],[-11506,4631,391,"182,199,255"],[-11470,-2559,290,"244,242,243"],[-11452,1915,374,"244,242,243"],[-11400,6151,273,"255,236,199"],[-11265,-2643,106,"255,212,120"],[-11244,2149,278,"255,235,196"],[-11227,198,382,"214,223,247"],[-11164,-7890,386,"255,236,198"],[-11103,-2822,282,"175,193,255"],[-11071,-1057,254,"215,224,246"],[-10968,3160,281,"255,243,221"],[-10928,3892,348,"255,236,199"],[-10783,-6903,191,"255,223,155"],[-10755,-5904,377,"255,220,145"],[-10746,-3429,229,"255,230,180"],[-10703,-3805,300,"175,193,255"],[-10692,-3802,356,"174,192,255"],[-10635,-4236,362,"255,224,159"],[-10558,938,319,"255,230,179"],[-10534,-5599,312,"255,220,146"],[-10493,3093,392,"205,218,255"],[-10280,6571,317,"187,203,255"],[-10241,-1572,243,"218,225,246"],[-10196,-4324,332,"255,248,238"],[-10134,1439,278,"255,230,178"],[-10124,2484,312,"220,227,246"],[-10124,3681,316,"255,223,156"],[-9950,-2500,327,"177,195,255"],[-9868,-5553,284,"255,222,152"],[-9865,-5638,331,"183,199,255"],[-9739,5230,279,"255,235,196"],[-9731,-3730,270,"178,196,255"],[-9723,-6068,360,"190,205,255"],[-9704,-4988,284,"185,201,255"],[-9660,-3710,162,"171,190,255"],[-9627,1256,208,"228,232,245"],[-9567,-4300,186,"255,249,241"],[-9560,-1540,354,"240,240,244"],[-9513,4601,382,"178,196,255"],[-9438,-3903,239,"179,197,255"],[-9413,457,276,"255,230,178"],[-9357,-6472,361,"255,230,179"],[-9339,2772,342,"255,240,213"],[-9310,-4013,299,"255,247,233"],[-9303,271,375,"216,224,246"],[-9254,-3704,319,"255,229,176"],[-9162,5687,373,"255,230,177"],[-9094,3725,386,"255,225,163"],[-9085,5149,224,"255,221,149"],[-9056,2925,370,"255,236,197"],[-9024,-977,332,"255,234,193"],[-8984,293,393,"214,223,246"],[-8855,-3042,298,"255,235,194"],[-8834,-5009,365,"190,206,255"],[-8816,956,371,"228,232,245"],[-8811,2876,384,"205,218,255"],[-8656,-2106,384,"232,235,244"],[-8559,-3676,310,"255,219,144"],[-8475,-2983,272,"255,224,161"],[-8474,7273,355,"255,247,234"],[-8467,-290,323,"255,236,197"],[-8408,2177,385,"255,230,178"],[-8396,-3438,179,"203,216,255"],[-8326,-4597,349,"178,196,255"],[-8301,-2542,282,"255,233,190"],[-8120,-824,385,"255,226,166"],[-8077,3878,3,"208,221,255"],[-7859,-2699,317,"189,205,255"],[-7748,3336,352,"211,222,247"],[-7618,-2630,205,"185,201,255"],[-7557,-2111,352,"255,230,180"],[-7526,3269,325,"199,213,255"],[-7435,-2988,260,"218,226,246"],[-7383,-2174,376,"255,234,191"],[-7365,1386,299,"213,222,247"],[-7344,-488,343,"191,206,255"],[-7326,-2767,332,"255,230,178"],[-7256,-2102,288,"252,248,242"],[-7186,6766,307,"255,234,193"],[-7072,5337,380,"255,235,196"],[-6958,-1785,392,"236,237,244"],[-6934,-4446,396,"193,208,255"],[-6903,-4062,396,"190,205,255"],[-6863,311,336,"246,244,243"],[-6757,5173,376,"227,232,245"],[-6732,2796,305,"255,232,185"],[-6376,4513,286,"208,221,255"],[-6344,1061,272,"255,221,150"],[-6315,1853,368,"255,226,166"],[-6296,7027,384,"255,237,201"],[-6230,887,76,"235,237,244"],[-6188,101,387,"255,244,223"],[-6117,641,371,"255,238,204"],[-6092,3508,389,"255,234,190"],[-6031,1949,351,"255,220,145"],[-5985,-7291,397,"202,216,255"],[-5782,-6618,355,"255,240,213"],[-5717,-82,324,"196,211,255"],[-5659,4674,380,"255,227,170"],[-5613,4771,396,"255,223,155"],[-5549,-1254,358,"255,237,202"],[-5475,-1478,305,"255,239,209"],[-5444,4026,223,"255,242,219"],[-5359,-5674,194,"187,203,255"],[-5061,1460,364,"255,249,240"],[-5061,-4729,311,"255,234,192"],[-5009,1591,377,"198,212,255"],[-4964,4528,125,"221,228,246"],[-4876,-6620,342,"229,233,245"],[-4868,6184,341,"255,236,199"],[-4845,3397,248,"255,234,190"],[-4808,-950,378,"211,221,247"],[-4630,-5845,367,"255,228,171"],[-4571,4117,394,"214,223,247"],[-4377,4393,372,"255,219,142"],[-4177,3023,321,"255,234,193"],[-4130,3805,374,"254,249,242"],[-4104,525,392,"255,246,229"],[-4036,6259,245,"239,239,244"],[-3833,-2241,377,"255,234,192"],[-3784,7056,323,"175,193,255"],[-3711,-557,290,"255,238,206"],[-3650,4559,398,"255,237,202"],[-3498,-1666,369,"246,244,243"],[-3463,-7739,373,"255,234,191"],[-3395,988,238,"255,221,149"],[-3324,-1613,285,"231,234,245"],[-3152,-3736,300,"193,208,255"],[-2855,-32,295,"255,235,195"],[-2825,2535,377,"255,249,239"],[-2794,-4696,173,"196,210,255"],[-2745,620,352,"220,227,246"],[-2729,5820,339,"255,220,146"],[-2537,-6026,287,"255,224,160"],[-2459,-139,386,"198,212,255"],[-2279,-2,365,"255,249,241"],[-2268,-4350,397,"255,234,190"],[-2218,5028,376,"215,224,246"],[-1963,1083,341,"193,208,255"],[-1933,-4688,207,"255,219,141"],[-1925,3022,293,"255,238,204"],[-1837,2357,397,"255,232,186"],[-1786,-5132,349,"220,227,246"],[-1758,6620,350,"255,233,188"],[-1750,2460,351,"255,236,198"],[-1685,-758,373,"255,218,140"],[-1634,-1582,327,"218,226,246"],[-1559,-2962,117,"227,232,245"],[-1452,4233,362,"191,206,255"],[-1406,2808,244,"255,217,138"],[-1381,1521,249,"208,221,255"],[-1264,-2117,368,"255,229,175"],[-1241,-4525,388,"255,234,192"],[-1071,328,370,"255,236,199"],[-1064,-5824,399,"255,249,241"],[-926,-2010,396,"255,232,185"],[-561,4646,381,"255,235,193"],[-516,7763,321,"255,233,189"]];



const THREE = await import('https://unpkg.com/three@0.172.0/build/three.module.js');

/* ══════════════════════════════════════════════════════════════════════
   Recovered from chunks/CY55ErfD.js, function Zr(canvas, props)
   ══════════════════════════════════════════════════════════════════════ */

const hero      = document.body;
const canvas    = document.getElementById('__bgScene');
const promptEl  = document.querySelector('.border-glow-card');
const wrapEl    = promptEl;

const note      = document.getElementById('__bgNote');
function fail(msg) {
  const b = document.getElementById('__bgFail');
  b.style.display = 'block';
  b.textContent = '背景引擎没起来：' + msg + '\n（这条横幅出现时，星空一定不会动）';
  throw new Error(msg);
}

const params = new URLSearchParams(location.search);
/* ?lens=0…2 — 0 disables the warp, 1 = Krea's original reach/strength */
const LENS = params.has('lens') ? Math.max(0, parseFloat(params.get('lens'))) : null;

/* ── 视角与纵深 ───────────────────────────────────────────────────────
   相机绕 (0,0,-4900) 转（半径 6000→600，Krea 的入场），指针叠加大幅环视。
   星图铺在半径 22000 的天球上、球心就在枢轴 —— 所以相机基本在球心，
   透视近似正交，星座不会被拉变形。

   纵深只有两条线索：
     1. 环视时天球整体扫过，粒子层按自己的轨道走 → 两者视差不同
     2. 粒子按距离失焦（近处散成光斑，远处才是锐点）→ 天球落在焦平面上  */
/* 视角控制分两条，因为它们是矛盾的：
     · 悬停要稳   → 光标不能同时当转视角用（1:1 跟随才稳，但那样只能扫 ±56°）
     · 要能看全   → 偏航得覆盖 360°、俯仰 ±90°
   所以：光标只做瞄准 + Krea 那点微动；**拖拽**才是自由环视，覆盖整个天球。 */
const LOOK         = 0.35;   // 指针微动幅度（rad），保留 Krea 的视差手感
const DRAG_SENS    = 0.0042; // 拖拽灵敏度 rad/px —— 1280px 约扫 308°
const DOF          = 1.0;    // 粒子失焦强度，0 = 全锐

const P = k => params.has(k) ? Math.max(0, parseFloat(params.get(k))) : null;
const LOOK_V   = P('look') ?? LOOK;
const DRAG_SENS_V = P('dragsens') ?? DRAG_SENS;
const DOF_V    = P('dof')  ?? DOF;
const DUST_V = P('dust');
// demo 默认就动：系统开着「减少动态效果」也能看见效果。?motion=off 关掉，F 键切换。
const FORCE_MOTION = params.get('motion') !== 'off';
const CAPTURE = params.has('capture');   // clock driven externally via __still(t)
let   userMotion = FORCE_MOTION;

const prefersReduce = () => matchMedia('(prefers-reduced-motion: reduce)').matches && !userMotion;
const reduced = () => prefersReduce();

/* ── renderer ──────────────────────────────────────────────────────── */
let renderer;
try {
  renderer = new THREE.WebGLRenderer({ canvas, depth:false, powerPreference:'low-power' });
} catch (e) {
  fail('WebGL 起不来 —— 浏览器可能关了硬件加速。\n错误：' + e.message);
  throw e;
}
renderer.setPixelRatio(Math.min(devicePixelRatio, 2));
renderer.toneMapping = THREE.ACESFilmicToneMapping;      // toneMapping = 2

const scene  = new THREE.Scene();
scene.background = new THREE.Color(0);
const camera = new THREE.PerspectiveCamera(58, 1, 30, 60000);
const target = new THREE.WebGLRenderTarget(1, 1, { type: THREE.HalfFloatType, depthBuffer:false });

/* ══════════════════════════════════════════════════════════════════════
   Two stacked layers, both living in the SAME 3D scene, so one render
   and one lens pass handles them together:

     L0  Krea's particle sky   — atmosphere, gives the lens depth to bend
     L1  the project's real sky — whole-sky map: 519 catalogue stars,
                                  88 IAU constellations, hover to light

   Putting L1 in world space rather than as a screen sticker is what makes
   both layers track the camera together; a pinned overlay would slide
   against the particles the moment the pointer moves.
   ══════════════════════════════════════════════════════════════════════ */

/* ── L0: Krea's particle sky (chunks/CY55ErfD.js, minus the "Agent" glyph) ── */
let lcg = 7391;                                   // LCG，跟 Krea 的星场同一个种子
const rnd = () => (lcg = (Math.imul(lcg, 1664525) + 1013904223) >>> 0) / 4294967296;

const POS = [], COL = [], STAR = [];
const _v = new THREE.Vector3(), _c = new THREE.Color();
const PARTICLE_DENSITY = Number(new URLSearchParams(location.search).get('density') ?? 1);
// 2.5 = 15000 点。密度上限由可读性门线倒推（见 #check 的 copyEffectivePeak / copyBusyPercent）
const COUNT = Math.round((canvas.clientWidth < 640 ? 1750 : 3000) * PARTICLE_DENSITY);

for (let i = 0; i < COUNT * 5; i++) {
  const far = i >= COUNT;
  _v.setFromSphericalCoords(
    far ? 10000 + rnd() * 10000 : 800 + Math.cbrt(rnd()) * 11000,
    Math.acos(rnd() * 2 - 1),
    rnd() * Math.PI * 2);
  if (i % 40 === 0 && !far) _v.multiplyScalar(0.25);
  POS.push(_v.x, _v.y, _v.z);
  STAR.push(
    far ? 0.15 + rnd() * 0.65 : 0.35 + Math.pow(rnd(), 2) * 3.3,
    far ? 0.02 + Math.pow(rnd(), 2) * 0.2 : 0.05 + Math.pow(rnd(), 3) * 8,
    rnd() * Math.PI * 2,
    0.8 + rnd() * 0.9);
  const r = rnd();
  const hue = r < 0.08 ? 0.06 + rnd() * 0.08 : r > 0.99 ? 0.83 + rnd() * 0.1 : 0.55 + rnd() * 0.1;
  _c.setHSL(hue, 0.5 + rnd() * 0.35, 0.64 + rnd() * 0.2, THREE.SRGBColorSpace);
  COL.push(_c.r, _c.g, _c.b);
}

const starGeo = new THREE.BufferGeometry();
starGeo.setAttribute('position', new THREE.Float32BufferAttribute(POS, 3));
starGeo.setAttribute('color',    new THREE.Float32BufferAttribute(COL, 3));
starGeo.setAttribute('star',     new THREE.Float32BufferAttribute(STAR, 4));

const starUniforms = {
  arrival:    { value: 0 },
  time:       { value: 0 },
  quiet:      { value: new THREE.Vector2(0.8, 0.7) },
  pixelRatio: { value: renderer.getPixelRatio() },
  dof:        { value: DOF },
  twinkle:    { value: Number(new URLSearchParams(location.search).get('twinkle') ?? 0.75) },
  // L1 carries the data now, so the atmosphere steps back instead of competing.
  dim:        { value: DUST_V ?? 0.30 },
};

const starMat = new THREE.ShaderMaterial({
  uniforms: starUniforms,
  transparent: true, depthWrite: false, depthTest: false, toneMapped: false,
  blending: THREE.AdditiveBlending,
  vertexShader: /* glsl */`
    attribute vec3 color;
    attribute vec4 star;
    uniform float arrival, pixelRatio, time, dim, dof, twinkle;
    varying vec3 tint;
    varying float energy, angle, aspect, spriteSize, foreground, defocus;
    void main() {
      vec4 view = modelViewMatrix * vec4(position, 1.0);
      gl_Position = projectionMatrix * view;
      float distance = max(1.0, -view.z);
      // 近处的尘埃该是失焦光斑，远处才是锐点 —— 这是纵深的唯一线索
      defocus = dof * (1.0 - smoothstep(900.0, 5200.0, distance));
      float diameter = clamp(star.x * 1400.0 / distance, 0.45, 3.8);
      foreground = arrival * smoothstep(1.2, 2.0, diameter) * (1.0 - smoothstep(1800.0, 3200.0, distance));
      spriteSize = diameter * 7.0 + 5.0;
      gl_PointSize = spriteSize * pixelRatio;
      energy = star.y * smoothstep(100.0, 400.0, distance) * dim;
      // 闪烁：幅度不乘 dim，否则压暗之后就看不出在动了
      float amp = twinkle * step(0.30, star.y) * step(0.55, fract(star.z * 3.77));
      energy *= 1.0 + arrival * amp
        * (0.60 + 0.40 * sin(time * (0.9 + star.w * 1.7) + star.z * 11.0));
      tint = color;
      angle = star.z;
      aspect = star.w;
    }`,
  fragmentShader: /* glsl */`
    varying vec3 tint;
    varying float energy, angle, aspect, spriteSize, foreground, defocus;
    void main() {
      vec2 pixel = (gl_PointCoord - 0.5) * spriteSize;
      float pinpoint = exp(-dot(pixel, pixel) * 0.5);
      vec2 p = gl_PointCoord * 2.0 - 1.0;
      p = mat2(cos(angle), -sin(angle), sin(angle), cos(angle)) * p;
      p *= vec2(inversesqrt(aspect), sqrt(aspect));
      // 失焦 = 核心的衰减指数塌下来，光散成一团；锐度只在远处保留
      float k = mix(65.0, 6.0, defocus);
      float core = exp(-dot(p, p) * k);
      vec2 shoulder = p - vec2(0.055, 0.025);
      float light = core + exp(-dot(shoulder, shoulder) * k * 0.55) * 0.22
        + exp(-dot(p, p) * 20.0) * 0.08 + exp(-dot(p, p) * 6.0) * 0.004;
      gl_FragColor = vec4((tint * light + vec3(1.0) * pinpoint * foreground * 3.0) * energy, 1.0);
    }`,
});
const stars = new THREE.Points(starGeo, starMat);
stars.frustumCulled = false;
scene.add(stars);

/* ══════════════════════════════════════════════════════════════════════
   L1 = the real sky, wrapped around the viewer as a sphere.

   一次只看得见约 14% 的天区 —— 这是要的，不是缺陷。整张星图铺满整个
   空间，随着视角变化才能逐块看到全部内容。投影本身的定义就是这样，
   想「整张同屏」就只能做成平面卡片，那是另一件事。
   ══════════════════════════════════════════════════════════════════════ */
/* ── the real sky ─────────────────────────────────────────────────────
   No random stars. This is the actual catalogue from the project
   (frontend/components/StarChart.tsx → demos/star-chart-data.js):
     519 stars, [RA*100, Dec*100, mag*100, "r,g,b"], real B–V colours,
     magnitudes −1.44 (Sirius) … +4.0.
   RA/Dec is mapped onto a sphere of radius SKY_R, so the Krea camera
   actually flies through the real sky. Size and brightness come
   straight from the magnitude — nothing is invented. */
/* The camera orbits (0,0,-4900) at a radius of 600…6000. Centring the sphere on
   the ORIGIN instead put the camera 48% of the way out, so perspective stretch
   mangled every constellation off-axis (measured: visible-star distance spread
   4581→9324, i.e. 2.0×). Centred on the pivot with a 22k radius that spread is
   ~1.03× and the sky reads as a proper map. */
const SKY_CENTER = new THREE.Vector3(0, 0, -4900);
const SKY_R = 22000;

const RAW_STARS = window.STARS;
const MAG_MIN = -1.44, MAG_MAX = 4.0;

/* celestial sphere: RA 0 / Dec 0 sits at (0,0,-R), i.e. dead ahead at t=0.
   The catalogue stores RA as degrees over the full ring (-180…180), the same
   convention the project's px() uses — NOT hours. */
function raDecToVec3(raDeg, decDeg, out) {
  const ra = (raDeg * Math.PI) / 180, dec = (decDeg * Math.PI) / 180;
  return out.set(
    SKY_CENTER.x + SKY_R * Math.cos(dec) * Math.sin(ra),
    SKY_CENTER.y + SKY_R * Math.sin(dec),
    SKY_CENTER.z - SKY_R * Math.cos(dec) * Math.cos(ra));
}

/* pure: catalogue magnitude → apparent diameter px (formula from StarChart.tsx) */
const magToDiameter = mag => Math.min(3.0, Math.max(0.7, 2.6 - mag * 0.42));
/* pure: catalogue magnitude → intrinsic brightness. Real range, no fudging. */
const magToEnergy = mag => 0.10 + 0.90 * Math.pow((MAG_MAX - mag) / (MAG_MAX - MAG_MIN), 1.15);

const ALWAYS_ON_MAG = 1.5;                 // ~23 颗常亮星，跟原项目一致

const SKY_POS = [], SKY_COL = [], SKY_STAR = [], GAIN = [];
const _sv = new THREE.Vector3(), _sc = new THREE.Color();

RAW_STARS.forEach((s, i) => {
  const ra = s[0] / 100, dec = s[1] / 100, mag = s[2] / 100;
  raDecToVec3(ra, dec, _sv);
  SKY_POS.push(_sv.x, _sv.y, _sv.z);
  const [cr, cg, cb] = s[3].split(',').map(n => Number(n) / 255);
  _sc.setRGB(cr, cg, cb, THREE.SRGBColorSpace);   // catalogue colours are sRGB
  SKY_COL.push(_sc.r, _sc.g, _sc.b);
  SKY_STAR.push(
    magToDiameter(mag),                      // x: apparent diameter
    magToEnergy(mag),                        // y: intrinsic brightness
    (i * 2.399963) % (Math.PI * 2),          // z: twinkle phase (golden-angle, stable)
    0.8 + ((i * 0.61803398875) % 1) * 0.9);  // w: aspect / twinkle speed
  GAIN.push(mag <= ALWAYS_ON_MAG ? 1 : 0);   // 常亮星置 1，其余由星座激活接管
});

const SKY_COUNT = RAW_STARS.length;

const skyGeo = new THREE.BufferGeometry();
skyGeo.setAttribute('position', new THREE.Float32BufferAttribute(SKY_POS, 3));
skyGeo.setAttribute('color',    new THREE.Float32BufferAttribute(SKY_COL, 3));
skyGeo.setAttribute('star',     new THREE.Float32BufferAttribute(SKY_STAR, 4));
const gainAttr = new THREE.Float32BufferAttribute(new Float32Array(GAIN), 1);
skyGeo.setAttribute('gain', gainAttr);
const gain = gainAttr.array;              // same caveat as lineAct above

const skyUniforms = {
  arrival:    { value: 0 },
  time:       { value: 0 },
  quiet:      { value: new THREE.Vector2(0.8, 0.7) },
  pixelRatio: { value: renderer.getPixelRatio() },
  // 星图作为应用背景要压到正文之下：0.42 = 看得清星座、又不跟正文抢
  skyDim:     { value: Number(new URLSearchParams(location.search).get('skydim') ?? 0.60) },
  skyTwinkle: { value: Number(new URLSearchParams(location.search).get('twinkle') ?? 0.75) },
};
const skyMat = new THREE.ShaderMaterial({
  uniforms: skyUniforms,
  transparent: true, depthWrite: false, depthTest: false, toneMapped: false,
  blending: THREE.AdditiveBlending,
  vertexShader: /* glsl */`
    attribute vec3 color;
    attribute vec4 star;
    attribute float gain;
    uniform float arrival, pixelRatio, time, skyDim, skyTwinkle;
    varying vec3 tint;
    varying float energy, angle, aspect, spriteSize, foreground;
    void main() {
      vec4 view = modelViewMatrix * vec4(position, 1.0);
      gl_Position = projectionMatrix * view;
      // Real catalogue stars are effectively at infinity: apparent size comes
      // from the magnitude alone, it must not change as the camera moves.
      float diameter = clamp(star.x, 0.5, 4.2);
      spriteSize = diameter * 7.0 + 5.0;
      gl_PointSize = spriteSize * pixelRatio;
      float tw = sin(time * (0.9 + star.w * 1.7) + star.z * 11.0);
      // gain: 1 for the ~23 always-on bright stars, otherwise the smoothed
      // activation of the constellation this star belongs to (0.16 floor).
      // 被点亮的星座和常亮星不受背景压暗影响 —— 悬停要一眼看得出来
      energy = star.y * arrival * mix(skyDim, 1.0, gain)
             * (1.0 + skyTwinkle * (0.5 + 0.5 * tw));
      foreground = arrival * gain * smoothstep(2.6, 3.6, star.x);
      tint = color;
      angle = star.z;
      aspect = star.w;
    }`,
  fragmentShader: /* glsl */`
    varying vec3 tint;
    varying float energy, angle, aspect, spriteSize, foreground;
    void main() {
      vec2 pixel = (gl_PointCoord - 0.5) * spriteSize;
      float pinpoint = exp(-dot(pixel, pixel) * 0.5);
      vec2 p = gl_PointCoord * 2.0 - 1.0;
      p = mat2(cos(angle), -sin(angle), sin(angle), cos(angle)) * p;
      p *= vec2(inversesqrt(aspect), sqrt(aspect));
      float core = exp(-dot(p, p) * 65.0);
      vec2 shoulder = p - vec2(0.055, 0.025);
      float light = core + exp(-dot(shoulder, shoulder) * 35.0) * 0.22
        + exp(-dot(p, p) * 20.0) * 0.08 + exp(-dot(p, p) * 6.0) * 0.004;
      gl_FragColor = vec4((tint * light + vec3(1.0) * pinpoint * foreground * 3.0) * energy, 1.0);
    }`,
});
const skyPoints = new THREE.Points(skyGeo, skyMat);
skyPoints.frustumCulled = false;
scene.add(skyPoints);

/* ── 88 real constellations, in 3D ────────────────────────────────────
   Lines and member stars come from the same IAU data as the project.
   Each constellation owns one scalar `act` (0→1). It rises when the cursor
   comes within HIT px of any of its projected vertices — that is the only
   thing that lights a constellation up, exactly like the original. */
const LINE_COLOR = new THREE.Color().setRGB(1, 224 / 255, 140 / 255, THREE.SRGBColorSpace); // 金色，取自 StarChart.tsx
const HIT = 55, SNAP = 14, STAR_HOVER = 90;

/* ---------- stars → 3D positions, once ---------- */
const starVec = RAW_STARS.map(s => {
  const v = new THREE.Vector3();
  return raDecToVec3(s[0] / 100, s[1] / 100, v);
});

/* ---------- constellations → 3D line segments ---------- */
const constellations = Object.entries(window.SKY).map(([id, segs]) => {
  const verts3 = [];                       // this constellation's vertices, in 3D
  const key = new Map();                   // dedupe shared vertices
  const lines = [];
  for (const seg of segs) {
    const run = seg.map(([ra, dec]) => {
      const k = ra.toFixed(2) + ',' + dec.toFixed(2);
      if (!key.has(k)) { const v = new THREE.Vector3(); key.set(k, raDecToVec3(ra, dec, v)); }
      return key.get(k);
    });
    for (let i = 1; i < run.length; i++) lines.push([run[i - 1], run[i]]);
    verts3.push(...run);
  }
  return { id, lines, verts3, act: 0, mid: new THREE.Vector3() };
});

const linePos = [], lineActIdx = [];        // per-vertex activation slot
for (let ci = 0; ci < constellations.length; ci++) {
  for (const [a, b] of constellations[ci].lines) {
    linePos.push(a.x, a.y, a.z, b.x, b.y, b.z);
    lineActIdx.push(ci, ci);
  }
}
const lineGeo = new THREE.BufferGeometry();
lineGeo.setAttribute('position', new THREE.Float32BufferAttribute(linePos, 3));
const lineActAttr = new THREE.Float32BufferAttribute(new Float32Array(lineActIdx.length), 1);
lineGeo.setAttribute('act', lineActAttr);
// three's Float32BufferAttribute *copies* the array you hand it, so keep a handle
// on the attribute's own store — writing into anything else uploads nothing.
const lineAct = lineActAttr.array;

const lineMat = new THREE.ShaderMaterial({
  uniforms: { tint: { value: LINE_COLOR }, opacity: { value: 0.55 }, uArrival: { value: 0 } },
  transparent: true, depthWrite: false, depthTest: false, toneMapped: false,
  blending: THREE.AdditiveBlending,
  vertexShader: /* glsl */`
    attribute float act;
    varying float vAct;
    void main() {
      vAct = act;
      gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }`,
  fragmentShader: /* glsl */`
    uniform vec3 tint; uniform float opacity; uniform float uArrival;
    varying float vAct;
    void main() {
      float a = vAct * uArrival;
      if (a < 0.004) discard;
      gl_FragColor = vec4(tint * a * opacity, 1.0);
    }`,
});
const lineMesh = new THREE.LineSegments(lineGeo, lineMat);
lineMesh.frustumCulled = false;
scene.add(lineMesh);

/* ---------- which catalogue stars belong to which constellation ---------- */
/* 成员星匹配：和原项目一样，用屏幕上 14px 内的距离来判定归属。 */
const starOwner = new Array(RAW_STARS.length).fill(-1);
{
  const v = new THREE.Vector3();
  constellations.forEach((c, ci) => {
    for (let si = 0; si < RAW_STARS.length; si++) {
      if (starOwner[si] !== -1 && RAW_STARS[si][2] / 100 <= ALWAYS_ON_MAG) continue;
      const p = starVec[si];
      if (c.verts3.some(q => q.distanceTo(p) < SKY_R * 0.016)) {   // ≈14px at the project's scale
        if (starOwner[si] === -1) starOwner[si] = ci;
      }
    }
  });
}

/* ---------- hover test: project vertices to CSS px, measure to the cursor ---------- */
const _p = new THREE.Vector3();
function isHot(mx, my, verts3, hit = HIT) {
  const w = cssW, h = cssH;
  for (const v of verts3) {
    _p.copy(v).project(camera);
    if (_p.z < -1 || _p.z > 1) continue;
    const dx = (_p.x * 0.5 + 0.5) * w - mx;
    const dy = (1 - (_p.y * 0.5 + 0.5)) * h - my;
    if (dx * dx + dy * dy < hit * hit) return true;
  }
  return false;
}

/* ---------- constellation names: a crisp 2D overlay, not refracted ---------- */
const labelCv = document.getElementById('__bgLabels');
const labelCtx = labelCv.getContext('2d');

let cssW = 1, cssH = 1;
function updateConstellations(mx, my, dt, arrival) {
  lineMat.uniforms.uArrival.value = arrival;

  for (const c of constellations)
    c.act += ((isHot(mx, my, c.verts3) ? 1 : 0) - c.act) * 0.08;

  for (let i = 0; i < lineActIdx.length; i++)
    lineAct[i] = constellations[lineActIdx[i]].act;
  lineActAttr.needsUpdate = true;
  window.__lineActMax = Math.max(...lineAct);

  // 常亮星永远 1；其余星跟着自己所属星座走，有 0.16 的底噪（其余星座暗下去）
  for (let si = 0; si < SKY_COUNT; si++) {
    const own = starOwner[si];
    gain[si] = RAW_STARS[si][2] / 100 <= ALWAYS_ON_MAG ? 1
             : Math.max(0.16, own === -1 ? 0 : constellations[own].act);
  }
  gainAttr.needsUpdate = true;

  // 名字：投影到屏幕，只画 act > 0.5 的
  const dpr = renderer.getPixelRatio();
  labelCtx.setTransform(dpr, 0, 0, dpr, 0, 0);
  labelCtx.clearRect(0, 0, cssW, cssH);
  labelCtx.font = '11px -apple-system, sans-serif';
  labelCtx.textAlign = 'center';
  for (const c of constellations) {
    if (c.act <= 0.5) continue;
    if (c.mid.lengthSq() === 0) {                  // centroid of its vertices
      c.mid.set(0, 0, 0);
      for (const v of c.verts3) c.mid.add(v);
      c.mid.multiplyScalar(1 / c.verts3.length);
    }
    _p.copy(c.mid).project(camera);
    if (_p.z < -1 || _p.z > 1) continue;
    labelCtx.globalAlpha = (c.act - 0.5) * 2;
    labelCtx.fillStyle = '#f2e3b3';
    labelCtx.fillText(c.id, (_p.x * 0.5 + 0.5) * cssW, (1 - (_p.y * 0.5 + 0.5)) * cssH - 8);
    labelCtx.globalAlpha = 1;
  }
}

/* ── gravitational-lens post pass ──────────────────────────────────── */
const lensUniforms = {
  tDiffuse:   { value: target.texture },
  resolution: { value: new THREE.Vector2(1, 1) },
  box:        { value: new THREE.Vector4() },      // centreX, centreY, halfW, halfH
  radius:     { value: 32 },
  amount:     { value: 0 },
  mass:       { value: 1 },
  time:       { value: 0 },
  arrival:    { value: 0 },
  quiet:      { value: new THREE.Vector2(0.8, 0.7) },
  // Reach / strength of the warp. Krea ships 45→115 px reach with a 0.45 pull and a
  // 1.8 bend — tuned for a dense particle cloud. Over 1px constellation lines that
  // band smears the shapes, hence the tighter defaults. `?lens=1` restores Krea's.
  lensNear:   { value: 16 },
  lensFar:    { value: 64 },
  lensPull:   { value: 0.35 },
  lensBend:   { value: 1.15 },
};

if (LENS !== null) {                       // ?lens=0 off · ?lens=1 Krea's original · 0…2 scales it
  lensUniforms.lensNear.value =  45 * LENS;
  lensUniforms.lensFar.value  = 115 * LENS;
  lensUniforms.lensPull.value = 0.45 * LENS;
  lensUniforms.lensBend.value = 1.80 * LENS;
}

const lensMat = new THREE.ShaderMaterial({
  uniforms: lensUniforms, depthTest: false, depthWrite: false,
  vertexShader: /* glsl */`
    varying vec2 vUv;
    void main() {
      vUv = uv;
      gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }`,
  fragmentShader: /* glsl */`
    uniform sampler2D tDiffuse;
    uniform vec2 resolution, quiet;
    uniform vec4 box;
    uniform float radius, amount, mass, arrival, time;
    uniform float lensNear, lensFar, lensPull, lensBend;
    varying vec2 vUv;
    void main() {
      vec2 p = vUv * resolution - box.xy;
      vec2 q = abs(p) - box.zw + radius;
      float edge = length(max(q, 0.0)) + min(max(q.x, q.y), 0.0) - radius;
      float horizon = max(box.w, 24.0);
      float impact = horizon + max(edge, 0.0);
      float einstein = horizon + 26.0 * mass;
      // Point-mass lens equation adapted to a rounded rectangle, not a Schwarzschild solution.
      float source = impact - einstein * einstein / impact;
      vec2 direction = normalize(p / max(box.zw, vec2(1.0)) + vec2(0.00001));
      // Krea's own numbers are p*0.45 / source*1.8 with a 45→115 px falloff. That is
      // tuned for a dense particle cloud; over 1px constellation lines the wide band
      // smears the shapes, so both the reach and the pull are dialled down here.
      vec2 bent = (box.xy + p * lensPull + direction * source * lensBend) / resolution;
      float influence = amount * (1.0 - smoothstep(lensNear, lensFar, edge));
      vec2 sampleUv = mix(vUv, bent, influence);
      vec2 bounds = min(sampleUv, 1.0 - sampleUv) * resolution;
      vec3 light = texture2D(tDiffuse, clamp(sampleUv, 0.0, 1.0)).rgb
        * smoothstep(0.0, 2.0, min(bounds.x, bounds.y));
      float clear = 1.0 - smoothstep(0.35, 1.0, length((vUv * 2.0 - 1.0 - vec2(0.0, 0.34)) / quiet));
      clear *= mix(1.0, smoothstep(12.0, 48.0, edge), amount);
      light *= pow(1.0 - arrival * clear * 0.98, 2.0);
      light *= mix(1.0, smoothstep(-1.0, 1.0, edge), amount);
      float ring = edge - 0.65;
      float pixel = max(fwidth(edge), 0.001);
      float coverage = clamp((ring + 0.25) / pixel + 0.5, 0.0, 1.0)
        - clamp((ring - 0.25) / pixel + 0.5, 0.0, 1.0);
      if (amount > 0.0 && abs(ring) < 3.0) {
        vec3 compressed = vec3(0.0);
        vec2 tangent = vec2(-direction.y, direction.x);
        for (int i = -16; i <= 16; i++) {
          float weight = exp(-pow(float(i) / 9.0, 2.0));
          for (int j = -1; j <= 1; j++) {
            vec2 ray = bent + (tangent * float(i) * 7.0 + direction * float(j) * 18.0 * mass) / resolution;
            compressed += texture2D(tDiffuse, clamp(ray, 0.0, 1.0)).rgb * weight;
          }
        }
        light += amount * (coverage + exp(-abs(ring) / 0.6) * 0.3) * compressed * 1.8;
      }
      float distance = max(edge, 0.0);
      float orbit = atan(direction.y, direction.x) + time * 0.56;
      float flow = 0.12 + 1.4 * pow(0.5 + 0.32 * sin(orbit * 2.0) + 0.18 * sin(orbit * 5.0), 2.0);
      float flare = max(exp(-distance / 20.0) - exp(-5.0), 0.0) * 0.035 * flow;
      light += amount * smoothstep(-0.5, 0.5, edge)
        * vec3(0.5, 0.72, 1.0) * flare;
      gl_FragColor = vec4(light, 1.0);
      #include <tonemapping_fragment>
      #include <colorspace_fragment>
    }`,
});

/* fullscreen triangle + ortho camera (class Yr in the bundle) */
const quadGeo = new THREE.BufferGeometry();
quadGeo.setAttribute('position', new THREE.BufferAttribute(new Float32Array([-1,3,0, -1,-1,0, 3,-1,0]), 3));
quadGeo.setAttribute('uv',       new THREE.BufferAttribute(new Float32Array([0,2, 0,0, 2,0]), 2));
const quadMesh = new THREE.Mesh(quadGeo, lensMat);
const quadScene = new THREE.Scene(); quadScene.add(quadMesh);
const quadCam = new THREE.OrthographicCamera(-1, 1, 1, -1, 0, 1);

/* ── pointer ───────────────────────────────────────────────────────── */
const pointer = new THREE.Vector2();   // smoothed, normalised −.5…−.5 (camera tilt)
const aim     = new THREE.Vector2();
const cursor  = { x: 0, y: 0 };        // CSS px inside the hero — drives the constellation layer
hero.addEventListener('pointermove', e => {
  const r = canvas.getBoundingClientRect();
  aim.set((e.clientX - r.left) / r.width - 0.5, 0.5 - (e.clientY - r.top) / r.height);
  cursor.x = e.clientX - r.left;
  cursor.y = e.clientY - r.top;
});
hero.addEventListener('pointerleave', () => {
  aim.set(0, 0);
  cursor.x = -9999; cursor.y = -9999;     // nothing hot
});

/* ── 拖拽环视：偏航不限，俯仰夹在 ±90°，这样整个天球都够得着 ────────── */
const view = { yaw: 0, pitch: 0, dragging: false, lastX: 0, lastY: 0 };
const clampPitch = v => Math.max(-Math.PI / 2 * 0.98, Math.min(Math.PI / 2 * 0.98, v));
hero.addEventListener('pointerdown', e => {
  if (e.target.closest('.prompt-box') || e.target.closest('button')) return;
  view.dragging = true; view.lastX = e.clientX; view.lastY = e.clientY;
  hero.setPointerCapture?.(e.pointerId);
  hero.style.cursor = 'grabbing';
});
hero.addEventListener('pointermove', e => {
  if (!view.dragging) return;
  view.yaw   -= (e.clientX - view.lastX) * DRAG_SENS_V;
  view.pitch  = clampPitch(view.pitch + (e.clientY - view.lastY) * DRAG_SENS_V);
  view.lastX = e.clientX; view.lastY = e.clientY;
  cursor.x = -9999; cursor.y = -9999;        // 拖拽时不点亮星座
});
const endDrag = () => { view.dragging = false; hero.style.cursor = ''; };
hero.addEventListener('pointerup', endDrag);
hero.addEventListener('pointercancel', endDrag);
window.__view = (yaw, pitch) => { view.yaw = yaw; view.pitch = clampPitch(pitch); };
window.__viewState = () => ({ yaw: +view.yaw.toFixed(3), pitch: +view.pitch.toFixed(3) });

/* engaged = the prompt box is focused or hovered → mass 1.35 */
let engaged = 0;
if (wrapEl) wrapEl.addEventListener('pointerenter', () => engaged = 1);
if (wrapEl) wrapEl.addEventListener('pointerleave', () => engaged = 0);
const editorEl = document.querySelector('.border-glow-card textarea') || document.querySelector('textarea');
editorEl.addEventListener('focus', () => engaged = 1);
editorEl.addEventListener('blur',  () => engaged = 0);

/* ── resize ────────────────────────────────────────────────────────── */
function resize() {
  const w = canvas.clientWidth || hero.clientWidth;
  const h = canvas.clientHeight || hero.clientHeight;
  if (!w || !h) return;
  renderer.setSize(w, h, false);
  target.setSize(canvas.width, canvas.height);
  camera.aspect = w / h;
  camera.updateProjectionMatrix();
  starUniforms.quiet.value.set(Math.min(0.9, 880 / w), 0.7);
  lensUniforms.resolution.value.set(w, h);

  cssW = w; cssH = h;
  const dpr2 = renderer.getPixelRatio();
  labelCv.width  = Math.round(w * dpr2);
  labelCv.height = Math.round(h * dpr2);

  if (cursor.x === 0 && cursor.y === 0) { cursor.x = w / 2; cursor.y = h / 2; }
}
new ResizeObserver(resize).observe(canvas);
resize();

/* 相机摆位单独成函数：自检要在不渲染的前提下扫遍整个天球，验证「看得到全部」 */
function placeCamera(p, m, h, g) {
  camera.position.set(
    Math.sin(h) * g + Math.sin(m * 0.03) * 6,
    Math.sin(h * 0.7) * 220 * CALM_BOB,          // 慢上下浮，配合自转形成"在星海里漂"
    -4900 + Math.cos(h) * g);
  camera.lookAt(0, 0, -4900);
  // 指针视差：这是最直接的"动态"——鼠标一动，两层一起走
  camera.rotateX(pointer.y * p * 0.45);
  camera.rotateY(pointer.x * p * 0.55);
  camera.rotateZ(Math.sin(m * 0.02) * 0.004);
  // 指针微动（Krea 的视差手感）+ 拖拽自由环视（覆盖整个天球）
  camera.rotateY(pointer.x * LOOK_V * p + view.yaw);
  camera.rotateX(pointer.y * LOOK_V * 0.55 * p + view.pitch);
  camera.updateMatrixWorld(true);
  camera.matrixWorldInverse.copy(camera.matrixWorld).invert();
}

/* ── the loop (verbatim control flow from the bundle) ──────────────── */
let elapsed = 0, last = 0, engagedDamped = 0, arrived = false;
/* 应用背景没有"入场"：相机停在一个固定的远轨道上，只做极慢的自转 + 指针视差。
   Krea 的 6000→600 飞入是落地页的东西，放在聊天界面里会让人分心。 */
const CALM_RADIUS = 2400;
const CALM_SPIN   = Number(new URLSearchParams(location.search).get('spin') ?? 0.055);  // rad/s ≈ 3°/s
const CALM_BOB    = Number(new URLSearchParams(location.search).get('bob') ?? 1);

function frame(now) {
  const dt = last && now ? (now - last) / 1000 : 0;
  last = now;
  const step = Math.min(dt, 0.05);

  const f = CAPTURE ? elapsed : (reduced() ? 4.4 : elapsed);
  const p = THREE.MathUtils.smootherstep(f, 0.8, 4.4);
  const m = Math.max(0, f - 4.4);
  engagedDamped = reduced() ? 0
                : THREE.MathUtils.damp(engagedDamped, engaged ? 1 : 0, 4, step);

  const h = elapsed * CALM_SPIN + engagedDamped * 0.05;
  const g = CALM_RADIUS;

  pointer.lerp(aim, 1 - Math.exp(-step * 4));
  placeCamera(Math.min(1, elapsed / 1.4), m, h, g);

  starUniforms.arrival.value = THREE.MathUtils.smootherstep(f, 1.8, 3.2);
  starUniforms.time.value = m;
  lensUniforms.amount.value = window.__lensOverride ?? (promptEl ? THREE.MathUtils.smootherstep(f, 2.2, 3.2) : 0);
  lensUniforms.mass.value = 1 + engagedDamped * 0.35;
  lensUniforms.time.value = m;
  lensUniforms.arrival.value = starUniforms.arrival.value;

  if (promptEl) {
    const cr = canvas.getBoundingClientRect();
    const pr = promptEl.getBoundingClientRect();
    lensUniforms.box.value.set(
      pr.left - cr.left + pr.width / 2,
      cr.bottom - pr.bottom + pr.height / 2,
      pr.width / 2, pr.height / 2);
    lensUniforms.radius.value =
      parseFloat(getComputedStyle(promptEl).borderTopLeftRadius) * pr.width / promptEl.offsetWidth;
  }

  if (!arrived && f >= 1.5) arrived = true;
  /* L1: 更新每个星座的激活量（投影到屏幕、判断光标距离），再重画标签层 */
  updateConstellations(cursor.x, cursor.y, reduced() && !CAPTURE ? 0 : step,
                       starUniforms.arrival.value);

  renderer.setRenderTarget(target);
  renderer.render(scene, camera);             // L0 + L1 into one buffer…
  renderer.setRenderTarget(null);
  renderer.render(quadScene, quadCam);        // …then one lens pass bends both together
}


const clock = { running: false };
function tick(now) {
  if (!clock.running) return;
  const dt = last && now ? (now - last) / 1000 : 0;
  if (!CAPTURE) elapsed += Math.min(dt, 0.05);
  frame(now);
  requestAnimationFrame(tick);
}
function start() {
  elapsed = CAPTURE ? 0 : (reduced() ? 4.4 : 0);
  last = 0; arrived = false;
    clock.running = true;
  requestAnimationFrame(tick);
}

/* ── motion control ────────────────────────────────────────────────── */
let btnMotion = null;   // 应用版没有这个按钮
function syncMotionUI() {
  const reduce = matchMedia('(prefers-reduced-motion: reduce)').matches;
  btnMotion = btnMotion || { textContent: '', setAttribute() {} };
  wrapEl.style.animationPlayState = reduced() ? 'paused' : 'running';
  const motionOn = !reduce || userMotion;
  note.innerHTML = '';    // 用 innerHTML 上色，一眼能看出是开是关
  note.textContent =
    `动态 ${motionOn ? '● 开' : '○ 关'}   ← 点这里 / 按 F 切换\n` +
    `系统 prefers-reduced-motion: ${reduce ? 'reduce（所以默认会停，这里已强制打开）' : 'no-preference'}\n` +
    `webgl: ${renderer.getContext().getParameter(WebGLRenderingContext.RENDERER)}\n` +
    `粒子 ${COUNT * 5} 点 · 真实星 ${RAW_STARS.length} 颗（星等 −1.44…+4.0）\n` +
    `星座 ${constellations.length} · 常亮星 ${RAW_STARS.filter(s => s[2] / 100 <= ALWAYS_ON_MAG).length}` +
    `\n拖动环视 · 悬停点亮星座`;
}
/* 应用版没有运动开关按钮，但保留 F 键强制动画——系统开了「减少动态效果」时也能看效果 */
const toggleMotion = () => { userMotion = !userMotion; last = 0; start(); syncMotionUI(); };
addEventListener('keydown', e => {
  if (e.key === 'f' || e.key === 'F') toggleMotion();
});
const noteEl = document.getElementById('__bgNote');
if (noteEl) noteEl.addEventListener('click', toggleMotion);
syncMotionUI();
start();

/* self-check: append #check and read document.title — deterministic. */
window.__render = t => { elapsed = t; last = 0; frame(performance.now()); };
window.__cursor = (x, y) => { cursor.x = x; cursor.y = y; };
window.__lensAmount = k => { window.__lensOverride = k; };
window.__engage = v => { engaged = v ? 1 : 0; };   // 捕获用：模拟鼠标移到输入框上
window.__view   = (yaw, pitch) => { view.yaw = yaw; view.pitch = pitch; };

if (location.hash === '#check') {
  setTimeout(() => {
    window.__render(3.6);
    const grab = () => {
      window.__render(3.6);
      const g2 = renderer.getContext();
      const W = canvas.width, H = canvas.height;
      const buf = new Uint8Array(W * H * 4);
      g2.readPixels(0, 0, W, H, g2.RGBA, g2.UNSIGNED_BYTE, buf);
      return buf;
    };
    const warm = b => {
      let n = 0;
      for (let i = 0; i < b.length; i += 4)
        if (b[i] > b[i + 2] + 12 && b[i] > 32 && b[i + 1] > b[i + 2] + 4) n++;
      return n;
    };
    const settle = (x, y) => { cursor.x = x; cursor.y = y;
      for (let i = 0; i < 70; i++) updateConstellations(cursor.x, cursor.y, 1 / 60, 1); };
    const lum = b => { let s = 0; for (let i = 0; i < b.length; i += 4) s += b[i] + b[i + 1] + b[i + 2]; return s / (b.length / 4) / 3; };

    settle(-9999, -9999);
    const bufIdle = grab();

    /* 对比度：正文区域必须留在可读区间 —— 这是「星星太多会不好看」的量化门 */
    const W = canvas.width, H = canvas.height;
    const strip = (buf, y0, y1) => {
      let mx = 0, sum = 0, n = 0;
      for (let y = (H * y0) | 0; y < (H * y1) | 0; y++)
        for (let x = 0; x < W; x++) {
          const i = (y * W + x) * 4;
          const v = Math.max(buf[i], buf[i + 1], buf[i + 2]);
          if (v > mx) mx = v; sum += v; n++;
        }
      return { peak: mx, mean: +(sum / n).toFixed(2) };
    };
    /* 读数要按"用户实际看到的"来 —— 画布上面还压着 .scrim 那层暗幕。
       这里把那条 CSS 渐变建模出来，算有效亮度，而不是拿裸画布的峰值吓自己。 */
    const mix = (a, b, t) => a + (b - a) * Math.min(1, Math.max(0, t));
    const scrimAlpha = (x, y) => {
      let v;
      if (y < 0.22) v = mix(0.72, 0.30, y / 0.22);
      else if (y < 0.62) v = 0.30;
      else v = mix(0.30, 0.80, (y - 0.62) / 0.38);
      let h;
      if (x < 0.12) h = 0;
      else if (x < 0.26) h = mix(0, 0.55, (x - 0.12) / 0.14);
      else if (x < 0.50) h = mix(0.55, 0.72, (x - 0.26) / 0.24);
      else if (x < 0.74) h = mix(0.72, 0.55, (x - 0.50) / 0.24);
      else if (x < 0.88) h = mix(0.55, 0, (x - 0.74) / 0.14);
      else h = 0;
      return 1 - (1 - v) * (1 - h);
    };
    // 只量正文真正落在的那一栏（消息列 max-width 820 / 视口 1440 ≈ 0.215…0.785）。
    // 两侧留白是背景最亮的地方，那是要的，不该算进可读性。
    const stripEff = (buf, y0, y1, x0 = 0.21, x1 = 0.79) => {
      let mx = 0, sum = 0, n = 0, busy = 0;
      for (let y = (H * y0) | 0; y < (H * y1) | 0; y++)
        for (let x = (W * x0) | 0; x < (W * x1) | 0; x++) {
          const i = (y * W + x) * 4;
          const raw = Math.max(buf[i], buf[i + 1], buf[i + 2]);
          const eff = raw * (1 - scrimAlpha(x / W, y / H));
          if (eff > mx) mx = eff;
          if (eff > 48) busy++;
          sum += eff; n++;
        }
      return { peak: +mx.toFixed(1), mean: +(sum / n).toFixed(2), busyPct: +(100 * busy / n).toFixed(3) };
    };
    const copyBand  = stripEff(bufIdle, 0.04, 0.88);   // 正文区
    const inputBand = stripEff(bufIdle, 0.88, 1.00);   // 输入区

    let target = null, tx = 0, ty = 0;
    outer:
    for (const cst of constellations)
      for (const v of cst.verts3) {
        _p.copy(v).project(camera);
        if (_p.z <= -1 || _p.z >= 1) continue;
        const x = (_p.x * 0.5 + 0.5) * cssW, y = (1 - (_p.y * 0.5 + 0.5)) * cssH;
        if (x > 200 && x < cssW - 200 && y > 120 && y < cssH - 220) { target = cst; tx = x; ty = y; break outer; }
      }
    let got = null;
    if (target) {
      const r = canvas.getBoundingClientRect();
      hero.dispatchEvent(new PointerEvent('pointermove', { clientX: r.left + tx, clientY: r.top + ty }));
      got = [cursor.x, cursor.y];
      settle(cursor.x, cursor.y);
    }
    const bufHot = grab();
    let changed = 0;
    for (let i = 0; i < bufHot.length; i += 4)
      if (Math.abs(bufHot[i] - bufIdle[i]) + Math.abs(bufHot[i + 1] - bufIdle[i + 1]) +
          Math.abs(bufHot[i + 2] - bufIdle[i + 2]) > 12) changed++;

    /* 输入框的透镜必须真的在弯折背后两层 */
    window.__cursor(-9999, -9999);
    window.__lensAmount(0);  const bufNoLens = grab();
    window.__lensAmount(1);  const bufLens   = grab();
    window.__lensAmount(null);
    let lensPx = 0, lensRingPx = 0, ringN = 0;
    // 输入框周围的环带（框外 16~64px）—— 透镜要在这里看得出来
    const cb0 = document.getElementById('composerBox').getBoundingClientRect();
    const cr0 = canvas.getBoundingClientRect();
    const dpr0 = canvas.width / cr0.width;
    const bx0 = (cb0.left - cr0.left) * dpr0, by0 = canvas.height - (cb0.bottom - cr0.top) * dpr0;
    const hw0 = cb0.width / 2 * dpr0, hh0 = cb0.height / 2 * dpr0;
    const cx0 = bx0 + hw0, cy0 = by0 + hh0;
    for (let i = 0; i < bufLens.length; i += 4) {
      const px = (i / 4) % canvas.width, py = Math.floor((i / 4) / canvas.width);
      const qx = Math.abs(px - cx0) - hw0, qy = Math.abs(py - cy0) - hh0;
      const edge = Math.max(qx, qy);
      const inRing = edge > -2 && edge < 64;
      if (inRing) ringN++;
      const d = Math.abs(bufLens[i] - bufNoLens[i]) + Math.abs(bufLens[i + 1] - bufNoLens[i + 1]) +
                Math.abs(bufLens[i + 2] - bufNoLens[i + 2]);
      if (d > 24) { lensPx++; if (inRing) lensRingPx++; }
    }
    window.__lensRing = { ringPx: lensRingPx, ringArea: ringN, pct: +(100 * lensRingPx / Math.max(ringN, 1)).toFixed(2) };

    const gl = renderer.getContext();
    const box = lensUniforms.box.value;
    const cb = document.getElementById('composerBox').getBoundingClientRect();
    const cr = canvas.getBoundingClientRect();
    const r = {
      three: THREE.REVISION,
      particles: COUNT * 5,
      particleDim: +starUniforms.dim.value.toFixed(3),
      skyDim: +skyUniforms.skyDim.value.toFixed(3),
      catalogueStars: RAW_STARS.length,
      constellations: constellations.length,
      alwaysOn: [...gain].filter((g, i) => RAW_STARS[i][2] / 100 <= ALWAYS_ON_MAG).length,
      // 输入框：透镜的质点必须落在真实输入框上，尺寸也要对得上
      lensTargetsInput: Math.abs(box.x - (cb.left - cr.left + cb.width / 2)) < 2 &&
                        Math.abs(box.z - cb.width / 2) < 2 &&
                        Math.abs(box.w - cb.height / 2) < 2,
      inputBox: [cb.width, cb.height].map(Math.round),
      lensRadius: +lensUniforms.radius.value.toFixed(1),
      // 可读性
      copyEffectivePeak: copyBand.peak, copyEffectiveMean: copyBand.mean,
      copyBusyPercent: copyBand.busyPct,
      inputEffectivePeak: inputBand.peak,
      lensPixelsMoved: lensPx,
      lensRingChanged: window.__lensRing?.ringPx, lensRingArea: window.__lensRing?.ringArea,
      lensRingChangedPct: window.__lensRing?.pct,
      hovered: target ? target.id : null,
      hoveredAct: target ? +target.act.toFixed(3) : -1,
      pixelsChangedOnHover: changed,
      pointerPlumbed: !!got && Math.abs(got[0] - tx) < 1 && Math.abs(got[1] - ty) < 1,
      reduce: matchMedia('(prefers-reduced-motion: reduce)').matches,
    };
    const ok = r.particles >= 5000 &&
               r.catalogueStars === 519 && r.constellations === 88 && r.alwaysOn === 23 &&
               r.lensTargetsInput && r.lensRadius === 34 &&            // = BorderGlow 的 borderRadius，不是 height/2
               r.copyEffectivePeak < 110 &&     // 暗幕之后，最亮的背景像素不能盖过正文（正文是 232）
               r.copyEffectiveMean < 9 &&       // 均值够暗，长时间阅读不累
               r.copyBusyPercent < 4 &&         // 正文区"花"的像素占比要低 —— 星星就是不能太多
               r.lensPixelsMoved > 800 && r.lensRingChanged > 2500 &&
               r.hoveredAct > 0.5 && r.pixelsChangedOnHover > 300 && r.pointerPlumbed;
    document.title = `${ok ? 'PASS' : 'FAIL'} ${JSON.stringify(r)}`;
  }, 2500);
}


  return 'ok';
})();

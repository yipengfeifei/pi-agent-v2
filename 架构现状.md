# Pi Agent V2 — 现状架构分析

> 基于对 `~/Desktop/助手加思考融合版` 的扫描与数天调试记录（2026-08-07）
> 目的：重构前的现状盘点，供讨论设计初衷与目标架构。

---

## 1. 运行时拓扑（三层）

```
Pi Agent.app（Swift 壳 PiNative，WKWebView 内嵌）
  └─ runtime.mjs（启动器）
       ├─ monitor-server.js  ── 端口 3000  「后端大脑」44 个 API + WebSocket + 状态机
       └─ server.js          ── 端口 30141 「前端」Next.js standalone（App 内 build）
              └─ 前端所有 /api/* 请求：部分直读文件（sessions/workflows），
                 大部分代理到 3000（orchestrate/task/agent-profiles/...）
```

- 开发模式（npm run dev）时前端跑在 3101，数据读 `~/.pi/agent`
- App 模式数据在 `~/Library/Application Support/Pi Agent/`（agent/、workspace/、workflows.json）

## 2. 后端模块与依赖（pi-backend/，共 ~28.7k 行）

```
monitor-server.js 14298 行 ← 一切的中心，import 几乎所有模块
├─ execution-planner-runtime.js   5328  规划器（judgment + plan 生成）
├─ execution-tool-runtime.js      1397  工具运行时（浏览器/文件/桌面副作用）
├─ software-capability-runtime.js 1302  软件能力发现（环境探测）
├─ personal-context-runtime.js    1281  个人上下文（记忆检索）
├─ execution-node-service.js       985  节点服务（条件/依赖求值）
├─ execution-runtime.js            639  执行运行时（验收标准/语义计划）
├─ execution-node-monitor-bridge.js 510  节点监控桥
├─ mcp-runtime.js / mcp-bootstrap-runtime.js   MCP 工具
├─ execution-goal-runtime.js / execution-effect-ledger.js / execution-context-runtime.js
├─ execution-session-runtime.js / task-complexity-runtime.js
├─ master-session-runtime.js       262  masterSession 状态（160 条消息截断）
├─ mission-envelope-runtime.js / mission-evidence-runtime.js  mission 封装
└─ 外部：@earendil-works/pi-ai、@earendil-works/pi-coding-agent
```

**核心结构问题**：`monitor-server.js` 一个文件承担了——HTTP 服务、44 个端点、orchestrate 状态机、DAG 执行、主助手对话（runAdaptiveAssistantTurn）、replan 调度、workflow 运行时、模型路由、个人记忆、训练接口。任何一层改动的根因都要在这 14k 行里顺藤摸瓜。

## 3. 三套会话模型（上下文失忆的根源）

| 会话类型 | 存储 | 谁在用 | 数据流 |
|---|---|---|---|
| 标准 agent 会话 | jsonl（sessions 目录） | Session 界面 `/classic` chat | useAgentSession → /api/agent + SSE |
| **masterSession** | workspace/<id>/master-session.jsonl + monitor 内存态（**160 条截断**） | Execute 界面 `/` | useOrchestrate → /api/orchestrate + WebSocket |
| workflow 运行 | workspace/<id>/ + ledger.jsonl + workflows.json | Flow 界面 `/classic` workflow | WorkflowEditor → /api/workflows/:id/run |

上下文在 masterSession 的截断（160 条）、过滤、压缩、replan 传递中层层损耗——**"失忆"不是模型问题，是三套模型之间的上下文传递链断了**（用户消息 → pendingReplan → recompileMasterPlanAtBoundary → leadPlan → runWeakExecutionPlanner → plan → executeReplannedSession → runAdaptiveAssistantTurn，每一步只带局部快照）。

## 4. 前端（pi-frontend/，共 ~26.7k 行）

```
/            → SimpleAssistantDialog（Execute）useOrchestrate source=session
/classic     → AppShell
                ├─ Session（chat）：ChatWindow + useAgentSession
                └─ Flow（workflow）：WorkflowEditor + useOrchestrate
                └─ 侧边栏：SessionSidebar（1500+ 行：会话树/cwd/删除/workflow 库/profiles/skills）

hooks：useAgentSession（38 处 effect）vs useOrchestrate（29 处）—— 两套会话 hook 并存
lib：session-reader / session-list / coding-*（另一套编码会话）/ workflow*
app/api：29 个 Next route，部分是直读文件的，部分是代理 3000 的
```

**重复维护点**：SessionSidebar（完整版）与 SimpleAssistantDialog 侧边栏（简化版，刚补了收缩/删除/cwd）逻辑重复；useAgentSession 与 useOrchestrate 状态模型不同步。

## 5. 后端 API 端点（44 个，monitor-server）

```
orchestrate 会话：/api/orchestrate、/:id/message、/abort、/confirm、/coach、/replan
task 控制：/api/task/:sessionId/:taskId/{abort,pause,resume,rerun,switch-model,promote-profile,promote-skills,save-experience}
workflow：/api/workflows（CRUD）、/:id/run、/resume、/from-session、/api/workflow-model/complete
状态/查询：/api/session-state/:id、/api/ledger/:id、/api/assistant-sessions、/api/resolve-session-file
扩展：agent-profiles、train、project-memory、guardian、skills、evals、monitor、multi-agent、workflow-recommend
```

## 6. 前端状态机（useOrchestrate）

```
guardian → planning → running → waiting_confirmation → done/error
                └→ replanning/judging/synthesizing/blocked（工作流内嵌）
```

一个用户消息进来要过：conversation gate → stage judgment → 规划 → DAG 调度 → 节点执行 → 合成 → 汇报，每层都有自己的状态和上下文快照。

## 7. 已知复杂度问题清单（调试中实际踩到的）

1. **单文件巨兽**：monitor-server.js 14k 行，44 端点 + 全状态机
2. **上下文失忆**：masterSession 160 条截断 + 系统消息淹没对话（已修：过滤 + 补历史，但架构上仍脆弱）
3. **replan 0 节点静默卡死**：规划器产出空任务且非 conversation 时无兜底（已修）
4. **三套会话 UI 三套数据源**：/api/sessions（文件）vs /api/assistant-sessions（内存态）vs workflow runs
5. **Execute 侧边栏重复造轮子**：与 SessionSidebar 逻辑重复
6. **删除路径断裂**：masterSession 会话 DELETE /api/sessions/:id 找不到文件
7. **前后端协议不统一**：直读文件 vs 代理 3000 混用

---

## 重构的讨论起点

- 用户反馈：功能上已经能跑（选品调研/工作流/会话），但**复杂度过高导致无法维护、上下文反复失忆**
- 设计初衷待用户补充：产品目标、目标用户、三个界面的本质分工、哪些是核心哪些是癌

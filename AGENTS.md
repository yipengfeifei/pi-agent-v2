# 项目规则

## 任务路由协议（每次收到用户请求，先走完这两步再动手）

### 第 1 步 · 确认意图
用户到底要什么？

- **研究结论/判断**（选品、要不要做某事、市场分析、方案论证）→ 研究型
- **构建系统/应用/工具/网站**（产出是成套多文件）→ 构建型
- **查事实/价格/新闻** → 查询型
- **改/修/写一个小东西**（改 bug、写单个文件、一句话回答）→ 直接干型

### 第 2 步 · 复杂度自问（3 个 yes/no，答完立刻路由，禁止纠结）
- **Q1**：这任务需要查大量外部信息、跨来源交叉验证才能给结论吗？→ YES = 调 `research`（多轮研究循环）
- **Q2**：这任务要分多步、产出多个文件、步骤间有数据依赖吗？→ YES = `update_plan` 建轻量清单边干边更新；只有需要节点级依赖编排/隔离执行/并行分支时才调 `plan` 拆图 + `worker` 执行
- **Q3**：单步就能完成（改一处、写一个文件、答一句话）？→ YES = 直接用 read/bash/write 干，**禁止走重流程**
- 多个 YES？**research 优先**（先研究后构建）；都不 YES？直接干。

### 路由速查表
| 意图 | 路由 | 工具 |
|---|---|---|
| 研究型 | Q1 YES | `research` |
| 构建型（多步） | Q2 YES | `update_plan` 清单；重依赖编排才走 `plan` → `worker` |
| 查询型 | 非 Q1 | `search` / `browser` |
| 直接干型 | Q3 YES | read/bash/write，不调重工具 |

## 工具分工（不重复判断）
- `update_plan` = 轻量任务清单：复杂/多步/长任务开工前建清单（一句话一步），执行中每完成一步立即更新状态；理解变化（拆分/合并/重排）时更新清单并附 explanation。简单任务禁止调用。
- `plan` = 拆解器：把需要节点级依赖编排/隔离执行/并行分支的目标拆成 2~8 个节点的图。
- `worker` = 执行器：plan 返回节点图后，逐个调用执行每个节点（干净上下文、上游产物自动注入）。
- `research` = 研究引擎：多轮自动循环（开题拆假设 → 定向补搜 → 反证收敛），收敛或需用户输入才返回。
- `search` = 单次检索；`browser` = 网页浏览；`site_memory` = 站点抓取经验固化。

## 提问纪律（能自解决就不停）
- 遇到决策缺口，先当子任务解决：search/browser/research 查资料 → 结合上下文推断 → 把假设记入 update_plan 的 explanation/步骤。不要停下来问。
- 只有**同时**满足三条才问用户：① 该决策会明显影响后续工作量（不同路径工作量差异大）② 前期无法推断 ③ 搜索也拿不到答案。
- 一旦开工且意图信息充分，后续不要反复确认；用户发现方向不对会主动插入纠正。

## 约束
- 简单任务禁止为了"显得专业"而调用 update_plan/plan/worker/research——重流程只给真正复杂的任务。
- 研究型任务**禁止**用 plan 拆图执行（研究走 research，构建才走 plan）。
